Subject: Review complete - APPROVED (evidence + findings)
Created: 2026-08-10T03:06:09
Updated: 2026-08-10T03:06:09
---
## Code review — CODEREVIEW-016 (foundry-streams CLI, 15 ops)

**Decision: APPROVED** — no corrections required. Reviewing `src/foundry_cli/streams/scripts/foundry_streams_cli.py`, `src/foundry_cli/streams/metadata-allow-list.md`, `.claude/skills/foundry-streams/`, `tests/test_foundry_streams_cli.py`, the shared `reset`-verb ACL change (`src/foundry_cli/common/access_control_guard.py`), and `pyproject.toml`.

### MANDATORY PRE-REVIEW VERIFICATION
- **Files exist**: `src/foundry_cli/streams/scripts/foundry_streams_cli.py`, `src/foundry_cli/streams/metadata-allow-list.md`, `.claude/skills/foundry-streams/SKILL.md`, `.claude/skills/foundry-streams/scripts/foundry_streams_cli.py`, `tests/test_foundry_streams_cli.py`, `tests/test_access_control_guard.py` — all physically present and tracked in commit `0c88063`.
- **Code compiles**: `python -m compileall -q src/foundry_cli/sql_queries src/foundry_cli/streams src/foundry_cli/common/access_control_guard.py` → exit 0.
- **Independent test run**: `pytest tests/test_foundry_sql_queries_cli.py tests/test_foundry_streams_cli.py tests/test_access_control_guard.py` → **131 passed, 0 failed**; coverage streams 90% branch, sql_queries 89%, ACL 96% (gate 80%). Note: an initial run showed 16 failures, root-caused to a leaked `FOUNDRY_AGENTIC_CLI_METADATA_ONLY=true` env var in the shell — the ACL correctly blocked all write ops under metadata-only mode (defense working as designed, not a defect). Re-run with the env var cleared: all green.
- **Lint**: `ruff check src/foundry_cli/sql_queries src/foundry_cli/streams src/foundry_cli/common/access_control_guard.py tests/*` → All checks passed.
- **Runtime probes**: `foundry-streams --help` exit 0 (exposes `dataset`/`stream`/`subscriber` resources); packaged metadata allow-list enforced (stream.get / stream.get_end_offsets / subscriber.get_read_position permitted under METADATA_ONLY, writes blocked) verified via direct AccessControlGuard probe.

### Findings (with file:line + code snippets)
1. **Operation catalog = exactly 15 (NOT stale 17)** — `OP_SPECS` at `foundry_streams_cli.py:117-253` contains exactly 15 unique entries (dataset 1 / stream 7 / subscriber 7); `test_catalog_contains_exact_15_operations` (test file:112-175) asserts `len(cli.OP_SPECS) == 15` and exact ordered pairs. Consistent with DESIGN-016's corrected count (the story title "17" is stale; vendored SDK v1.102.0 exposes 15; canonical env-var reference and allow-list concordant at 15).
2. **SDK dispatch paths** — `_get_client` (line 263-269) resolves `root_client.streams` then walks `client_path`: `Dataset` (dataset.create), `Dataset.Stream` (7 stream ops), `Dataset.Stream.Subscriber` (7 subscriber ops). Dispatch tests assert exact awaited kwargs, e.g. `test_dataset_create_dispatches_with_json_schema` (line 300-326) `assert_awaited_once_with(name=..., parent_folder_rid=..., schema={...}, request_timeout=120)`.
3. **Argument validation** — `_validate_inputs` (line 425-446) validates scalars/JSON/batch bounds before client creation; `test_parser_rejects_unknown_operation` (line 251-253) and missing-file rejection test (line 452-468, `factory.create_calls == 0`).
4. **ACL write classification incl. `reset` verb** — `_WRITE_VERBS` in `access_control_guard.py:33-78` now includes `"reset"` (comment lines 74-76: "stream.reset and subscriber.reset_offsets destroy or rewind stream state"); `_is_write_operation` (access_control_guard.py:149-172) prefix-matches `reset` → `reset` and `reset_offsets` classified as writes. Write set of 10 ops verified: `test_readonly_blocks_ten_write_operations` (test file:584-609) exercises all 10 including `stream reset` and `subscriber reset-offsets` → exit 8, `factory.create_calls == 0`. `test_reset_verbs_stay_write_classified_under_narrow_overrides` (test file:640-682) proves narrower `ENABLED=true`/`METADATA_ONLY=false` overrides cannot downgrade the write classification. Cross-namespace grep confirms no other namespace has a `reset*` op → additive-only, no regression.
5. **Metadata-only policy 3/12** — `streams/metadata-allow-list.md` permits exactly `streams.stream.get`, `streams.stream.get_end_offsets`, `streams.subscriber.get_read_position` (3 PERMITTED rows of 15); `test_metadata_only_permits_exactly_3_blocks_12` (test file:695-737) parses the packaged file and asserts the exact permitted set; runtime probe confirms.
6. **ADR-003 batch strategy** — `BATCH_READ_OPS` (line 272-276) = `("stream","get_records"): 10_000`, `("subscriber","read_records"): 1_000`; `--max-records` default 100 (`_DEFAULT_MAX_RECORDS` line 73) registered only on batch ops (line 331-339); `_read_max_records` (line 399-408) bounds 1..max; `_build_kwargs` maps `--max-records` → SDK `limit` (line 480-482). Tests: `test_catalog_marks_exactly_two_batch_read_operations` (test file:177-183), `test_parser_rejects_max_records_above_bound` (line 255-270, 10001 and 1001 rejected), `test_get_records_batch_read_maps_max_records_to_limit` (line 339-362, `limit=7` awaited), `test_subscriber_read_records_aggregates_batch` (line 364-401, `auto_commit=True, partition_ids=["0"], limit=5`).
7. **Streams timeout env** — `_STREAMS_TIMEOUT_ENV = "FOUNDRY_AGENTIC_CLI_STREAMS_TIMEOUT_S"` (line 69), `_STREAMS_DEFAULT_TIMEOUT_S = 120` (line 71); `main` (line 570-574) reads `cfg.get_int(_STREAMS_TIMEOUT_ENV, None)` falling back to 120; `_validate_timeout` bounds 1..3600 (line 392-397). Tests: `test_streams_default_timeout_used_when_env_absent` (line 814-829, `request_timeout=120` awaited), `test_streams_timeout_env_overrides_default` (line 831-857, `request_timeout=45`).
8. **B3 tracing + no-attribution** — `invocation_scope(cfg, include_attribution=False)` / `factory.create(cfg, include_attribution=False)` (lines 591-592); `test_invocation_uses_include_attribution_false` (line 742-759).
9. **Retry/timeout ADR-002** — `RetryHandler(timeout_s=timeout)` wraps invocation (line 594-595); `test_timeout_accepts_adr_002_bounds` (line 797-804) rejects 0/3601/-1/True.
10. **Binary publish bounded read** — `_read_file_bounded` (line 411-422): `path.is_file()` check, 16 MiB cap via `path.stat().st_size` comparison, `read_bytes`; invoked AFTER ACL decision and BEFORE client construction (lines 585-588, `setattr(args, "_file_bytes", ...)`); `_invoke` appends bytes positional (line 495) and `_build_kwargs` skips `file` (line 476-479). Tests: `test_publish_binary_record_reads_file` (line 437-450, `assert_awaited_once_with(..., b"binary-content", ...)`), `test_publish_binary_record_missing_file_rejected_before_client` (line 452-468).
11. **Error serialization ADR-001** — `_serialize_error` (line 501-548) same taxonomy as sql-queries; `test_sdk_error_maps_to_exit_code` (line 880-889, TimeoutError→5); `test_sensitive_values_not_echoed_in_errors` (line 949-961, "secret-token" redacted, message "Streams operation failed").
12. **Output formats ADR-004 / logging ADR-005** — `OutputFormatter` (line 597), stdout only; `LogSetup.configure` (line 567). `test_output_toon_and_json_formats` (line 891-904).
13. **Console boundary** — single `asyncio.run(main())` (line 608-614); `_ArgumentParser.error` → structured error (line 91-93); `test_console_main_uses_one_asyncio_run_boundary` (line 906-918).
14. **No pagination** — no ResourceIterator in Streams SDK surface; no PaginationHelper/flags. Matches DESIGN-016 paging contract (batch reads bounded by `--max-records`; callers iterate).
15. **pyproject entry points/package-data** — `[project.scripts]` `foundry-streams = "foundry_cli.streams.scripts.foundry_streams_cli:console_main"`; package-data `"foundry_cli.streams" = ["metadata-allow-list.md"]`; ruff per-file-ignores E402 for streams scripts.
16. **Security (OWASP)** — no credentials; input validated at all entry points; error messages static/redacted; file read bounded (16 MiB) preventing resource exhaustion; no prompt-injection surface; at-least-once retry semantics disclosed for writes (duplicate records/cost risk documented in skill).
17. **Code quality** — catalog-driven (single `_op` builder), DRY (shared `_add_kwarg`/`_build_kwargs`/`_validate_inputs`), full type annotations, docstrings, no dead code or commented-out blocks.

### Non-blocking observations (no action required)
- `subscriber.read_records` returns `ReadSubscriberRecordsResponse` and is emitted as a single object (not an array) — per DESIGN-016 table row 14 this is the SDK contract; fine.
- `runpy` RuntimeWarning when invoking via `python -m` (package `__init__.py` re-export double-import) — benign; console entry point path unaffected.
- `test_readonly_blocks_stream_reset_before_client` (line 684-693) duplicates coverage already present in the 10-op list test; harmless.

### Conclusion
All DoD criteria met: doc study (DESIGN-016, ADR-003 batch strategy, DESIGN-011/012/013 patterns, ADR-001..007), critical thinking applied, no open questions, findings cite specific file:line + code snippets, decision made. **APPROVED — move CODEREVIEW-016 to Closed and return DEV-016 to prior status (Resolved), then close DEV-016.**
