Subject: Review complete - APPROVED (evidence + findings)
Created: 2026-08-10T03:05:33
Updated: 2026-08-10T03:05:33
---
## Code review — CODEREVIEW-015 (foundry-sql-queries CLI, 5 ops)

**Decision: APPROVED** — no corrections required. Reviewing `src/foundry_cli/sql_queries/scripts/foundry_sql_queries_cli.py`, `src/foundry_cli/sql_queries/metadata-allow-list.md`, `.claude/skills/foundry-sql-queries/`, `tests/test_foundry_sql_queries_cli.py`, and the shared `access_control_guard.py` change.

### MANDATORY PRE-REVIEW VERIFICATION
- **Files exist**: `src/foundry_cli/sql_queries/scripts/foundry_sql_queries_cli.py`, `src/foundry_cli/sql_queries/metadata-allow-list.md`, `.claude/skills/foundry-sql-queries/SKILL.md`, `.claude/skills/foundry-sql-queries/scripts/foundry_sql_queries_cli.py`, `tests/test_foundry_sql_queries_cli.py`, and pyproject entries verified via file search/listing — all physically present and tracked in commit `0c88063`.
- **Code compiles**: `python -m compileall -q src/foundry_cli/sql_queries src/foundry_cli/streams src/foundry_cli/common/access_control_guard.py` → exit 0.
- **Independent test run**: `pytest tests/test_foundry_sql_queries_cli.py tests/test_foundry_streams_cli.py tests/test_access_control_guard.py` → **131 passed, 0 failed**; coverage sql_queries 89% branch, streams 90%, ACL 96% (gate 80%). Note: an initial run showed 16 failures, root-caused to a leaked `FOUNDRY_AGENTIC_CLI_METADATA_ONLY=true` env var in the shell — the ACL correctly blocked all write ops under metadata-only mode (defense working as designed, not a defect). Re-run with the env var cleared: all green.
- **Lint**: `ruff check src/foundry_cli/sql_queries src/foundry_cli/streams src/foundry_cli/common/access_control_guard.py tests/*` → All checks passed.
- **Runtime probes**: `foundry-sql-queries --help` exit 0; packaged metadata allow-list enforced (get_status permitted under METADATA_ONLY, others blocked) verified via direct AccessControlGuard probe.

### Findings (with file:line + code snippets)
1. **Operation catalog = exactly 5** — `OP_SPECS` at `foundry_sql_queries_cli.py:116-157` contains exactly 5 unique entries (cancel/execute/execute_ontology/get_results/get_status); `test_catalog_contains_exact_5_operations` (test file:70-100) asserts `len(cli.OP_SPECS) == 5`. Matches DESIGN-015. NOT the stale 17 count (that was streams).
2. **SDK dispatch paths** — `_get_client` (line 223-229) resolves `root_client.sql_queries` then walks `client_path`; all 5 ops route through `("SqlQuery",)` (lines 121-153). Single client path per DESIGN-015. Dispatch tests (line 191-232) assert exact `assert_awaited_once_with` kwargs.
3. **Argument validation** — `_validate_inputs` (line 318-331) validates non-empty strings and decodes JSON before any client creation; `test_invalid_*_json_rejected_before_client` (line 281-310) asserts `factory.create_calls == 0`.
4. **ACL classification** — write set `cancel`, `execute`, `execute_ontology` (lines 116-157); all three already in `_WRITE_VERBS` (access_control_guard.py:33-78 — `cancel`, `execute`). `get_results`/`get_status` semantic reads. `test_readonly_blocks_three_write_operations` (line 402-424) and `test_semantic_reads_permitted_under_readonly` (line 426-462) verify both directions; `test_acl_denial_reports_rule` (line 517-530) verifies metadata-only blocking with message.
5. **Metadata-only policy 1/4** — `metadata-allow-list.md` permits exactly `sql_queries.sql_query.get_status`; `test_metadata_only_permits_exactly_1_blocks_4` (line 532-556) parses the packaged file and asserts the exact permitted set; runtime probe confirms.
6. **Arrow downloads (streaming downloads)** — `DOWNLOAD_OPS` (line 69-73) = `execute_ontology`, `get_results`; `_download_operation` (line 373-402) uses `client.with_streaming_response.<op>` async context manager and `BinaryDownloadHandler.save(...)` with `content_length=None` for unknown-length probing, writes before opening destination; tests `test_execute_ontology_writes_atomically...` (line 352-375) and `test_get_results_download_requires_output...` (line 377-401) verify atomic persistence and response close (`streaming.ctx.closed`). `execute_ontology` has no `--output`; `get_results` accepts it. Unsafe filename rejection verified (line 403-419).
7. **B3 tracing + no-attribution** — `invocation_scope(cfg, include_attribution=False)` and `factory.create(cfg, include_attribution=False)` (lines 483-484); `test_invocation_uses_include_attribution_false` (line 568-582) asserts `scope_kwargs`/`create_kwargs` = `{"include_attribution": False}`.
8. **Retry/timeout ADR-002** — `_validate_timeout` (line 301-306) bounds 1..3600s; `RetryHandler(timeout_s=timeout)` wraps the invocation (lines 486-494); `test_timeout_accepts_adr_002_bounds` (line 585-592) and `test_invalid_timeout_stops_before_acl_or_client` (line 594-606).
9. **Error serialization ADR-001** — `_serialize_error` (line 404-451) maps declared exit_code → sdk exit → HTTP status (401/403/404/429) → ConfigurationError → ValueError/TypeError → TimeoutError → server; `test_sdk_error_maps_to_exit_code` (line 620-630) verifies TimeoutError→5; `test_sensitive_values_not_echoed_in_errors` (line 668-677) verifies message redaction ("secret-token" not in envelope).
10. **Output formats ADR-004 / logging ADR-005** — `OutputFormatter(format_setting=args.format, pretty=args.pretty)` (line 496), stdout only for results; `LogSetup.configure` (line 468); NDJSON diagnostics to stderr. `test_output_toon_and_json_formats` (line 632-646).
11. **Console boundary** — `console_main` uses one `asyncio.run(main())` boundary (line 508-514); `_ArgumentParser.error` (line 84-86) routes argparse failures into structured error path; `test_console_main_uses_one_asyncio_run_boundary` (line 648-660); `test_parser_rejects_unknown_operation` (line 148-151) and `test_missing_operation_returns_user_input_error` (line 266-279).
12. **No pagination** — verified: no `PaginationHelper`, no `--page-size`/`--page-token`/`--all`/`--max-pages` flags (SqlQueries has no ResourceIterator). Matches DESIGN-015 paging contract.
13. **pyproject entry points/package-data** — `pyproject.toml` `[project.scripts]` `foundry-sql-queries = "foundry_cli.sql_queries.scripts.foundry_sql_queries_cli:console_main"`; package-data `"foundry_cli.sql_queries" = ["metadata-allow-list.md"]`; ruff per-file-ignores E402 added for the namespace scripts.
14. **Security (OWASP)** — no credentials in code; input validated at all entry points (JSON/scalars/timeout); error messages never echo values (`_required_text`/`CLIInputError` messages are static); redaction test passes; no prompt-injection surface (all args are SDK params); bounded download via BinaryDownloadHandler. Cross-namespace impact of the shared `reset` verb: no other namespace has a `reset*` op (grep across `src/foundry_cli`), so the ACL change is additive-only.
15. **Code quality** — SOLID/KISS/DRY: catalog-driven parser/dispatch (single `_op` builder, shared `_add_kwarg`/`_build_kwargs`), no dead code, full type annotations, docstrings present. Immutable catalog, no commented-out blocks.

### Non-blocking observations (no action required)
- `_model_to_dict` bytes branch (line 345-349) is untested — defensive only (no bytes result path for SqlQueries non-download ops); acceptable.
- `runpy` RuntimeWarning when invoking via `python -m` (package `__init__.py` re-export double-import) — benign; the console entry point path is unaffected.

### Conclusion
All DoD criteria met: doc study (DESIGN-015, DESIGN-005/010/011/012/013 patterns, ADR-001..007), critical thinking applied, no open questions, findings cite specific file:line + code snippets, decision made. **APPROVED — move CODEREVIEW-015 to Closed and return DEV-015 to prior status (Resolved), then close DEV-015.**
