Subject: Unblocked — corrected widgets surface: tests target 8 ops (QUESTION-043 decision, LINK-00678 removed)
Created: 2026-08-11T00:50:35
Updated: 2026-08-11T00:50:35
---
---
## Unblocked by tech-lead decision on QUESTION-043 (2026-08-11)

Blocks link LINK-00678 removed. **Unit tests must target the 8-op catalog below — NOT the stale 12-op DESIGN-022 count.**

### Corrected catalog (verified against installed foundry-platform-sdk 1.102.0)
- `dev-mode-settings enable`, `dev-mode-settings set-widget-set-by-id` (2)
- `repository get`, `repository publish` (2; publish = bounded zip upload)
- `widget-set get` (1)
- `release delete`, `release get`, `release list` (3; list = cursor-paged via PaginationHelper)

`DevModeSettingsV2` is OUT of scope (new SDK resource, no design).

### Test plan adjustments
1. Catalog integrity test: `len(cli.OP_SPECS) == 8`.
2. Parser surface + dispatch for each op (incl. `--settings-json` for set_widget_set_by_id, `--file`/`--repository-version` for publish).
3. Bounded zip upload for `repository publish` (16 MiB after ACL before client).
4. Pagination for `release list` (with_raw_response + PaginationHelper).
5. ACL write classification per final write set; metadata-only policy per the amended 8-row allow-list.
6. `include_attribution=False`, error taxonomy, timeout, output format, console boundary.
---
