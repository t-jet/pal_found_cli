Subject: Review evidence — APPROVED (CODEREVIEW-022)
Created: 2026-08-11T02:50:50
Updated: 2026-08-11T02:50:50
---
## Review evidence — CODEREVIEW-022 APPROVED (tech-lead, 2026-08-11)

### Pre-review verification (MANDATORY gate)
- HEAD = 1b15565 "feat(widgets): add foundry-widgets CLI - 8-op corrected catalog (DEV-022, UNITTEST-022)"; all implementation files committed (`src/foundry_cli/widgets/`, `.claude/skills/foundry-widgets/`, `tests/`, `pyproject.toml`). Working tree shows no implementation diffs (only tracker/deliverable artifacts).
- `compileall src` exit 0; `ruff check` clean (widgets + ACL guard + tests); `mypy` 0 errors on `src/foundry_cli/widgets` and `src/foundry_cli/common/access_control_guard.py`.
- Focused suite: 122 passed (`tests/test_foundry_widgets_cli.py` 38 + `tests/test_widgets_console_wrapper.py` 3 + `tests/test_access_control_guard.py` 81). Full suite: **1362 passed**, 86.55% branch (widgets namespace 85% branch).

### Catalog — 8-op corrected surface per QUESTION-043 (verified)
- `src/foundry_cli/widgets/scripts/foundry_widgets_cli.py` OP_SPECS (L128-188): exactly 8 entries — dev_mode_settings.enable (L129), dev_mode_settings.set_widget_set_by_id (L135; required widget_set_rid + settings, settings in json_args), release.delete (L143), release.get (L150), release.list (L157), repository.get (L164), repository.publish (L171; required repository_version + file), widget_set.get (L179). Stale-12 ops (disable/get/pause/set_widget_set) and DevModeSettingsV2 absent. Tests: `test_catalog_contains_exact_8_operations` (L163), `test_catalog_excludes_missing_dev_mode_settings_operations` (L214).
- Client mounting: `_get_client` (L268-274) roots at `root_client.widgets` then walks `client_path`; release.* resolves through the nested `("WidgetSet", "Release")` mount (SDK 1.102.0 `AsyncWidgetSetClient.Release` cached_property — confirmed via inspect).

### SDK dispatch (installed foundry-platform-sdk 1.102.0, inspect.signature)
- `DevModeSettings.enable(*, preview, request_timeout)` — CLI passes only `request_timeout` (correct).
- `set_widget_set_by_id(*, settings: WidgetSetDevModeSettingsById, widget_set_rid, ...)` — CLI forwards `--widget-set-rid` + decoded `--settings-json` dict (correct).
- `Repository.get(repository_rid)`; `publish(repository_rid, body: bytes, *, repository_version, ...)` — CLI appends `_file_bytes` positionally (`_invoke` L375-376) and forwards `repository_version` (correct).
- `WidgetSet.get(widget_set_rid)` (correct).
- `Release.delete/get(widget_set_rid, release_version)` — both positionals (correct); `Release.list(widget_set_rid, *, page_size, page_token)` returns `AsyncResourceIterator` (correct).
- No `preview`/`_sdk_internal`/`attribution` forwarded; `include_attribution=False` on both `invocation_scope` and `create` (L541-543).

### ACL — new verbs and policy (verified)
- `src/foundry_cli/common/access_control_guard.py` `_WRITE_VERBS` L97-98: `enable` + `set_widget_set` added; prefix match (L188) covers `set_widget_set_by_id`; `delete` (L36) and `publish` (L55) already present. Regression test `test_widgets_verbs_are_writes` (`tests/test_access_control_guard.py` L230-244).
- Runtime probe under METADATA_ONLY: all 4 writes (enable, set-widget-set-by-id, release delete, repository publish) blocked with exit 8 before client creation; all 4 reads (repository get, widget-set get, release get, release list) pass ACL.
- Packaged `src/foundry_cli/widgets/metadata-allow-list.md`: exactly 8 rows, 4 PERMITTED / 4 BLOCKED, consistent with canonical `.ept/docs/deliverables/architecture/metadata-allow-list.md` widgets rows (4 stale rows marked not-implemented).

### Binary upload — repository publish (verified)
- Bounded read 16 MiB (`_UPLOAD_MAX_BYTES` L86) via `_read_file_bounded` (L305-314); executed after `AccessControlGuard.check` (L524-527) and before `AsyncClientFactory` (L539). `--file` consumed locally, never forwarded; `--repository-version` forwarded as SDK `repository_version`. Tests: `test_repository_publish_reads_file_bounded` (L530), `test_repository_publish_rejects_missing_file` (L566), `test_repository_publish_rejects_oversized_file` (L593).

### Pagination — release list (verified)
- `PAGINATED_OPS = {("release", "list")}` (L82); `_paginate_operation` (L414-448) uses `client.with_raw_response.list` (L418) + `PaginationHelper`; flags `--page-size/--page-token/--all/--max-pages` (L201-213); default 1 page, `--all` selects `HARD_MAX_BATCH_PAGES`. Tests: `test_release_list_uses_raw_response_and_helper` (L629), `test_release_list_defaults_to_single_page` (L649).

### ADR compliance (verified)
- ADR-001: `_serialize_error` (L450-498) maps exit codes 1..9 (user 1, auth 2, permission 3, not-found 4, timeout 5, server 6, 429 7, ACL 8, config 9); JSON envelope to stdout; ACL handled at L562-564. Tests: `test_sdk_error_maps_to_server_error_exit_code` (L896), `test_sdk_timeout_maps_to_timeout_exit_code` (L913).
- ADR-002: `_validate_timeout` (L298-303) bounds 1..3600; `request_timeout` forwarded (L365); `RetryHandler` wraps invocation (L544).
- ADR-004: `--format json|toon|auto` + `--pretty` via `OutputFormatter` (L549-552).
- ADR-005: `LogSetup.configure` (L532); NDJSON diagnostics via logger.

### B3 tracing / no-attribution (verified)
- `factory.invocation_scope(cfg, include_attribution=False)` + `factory.create(cfg, include_attribution=False)` (L541-543); SDK-native B3. Test `test_invocation_uses_include_attribution_false` (L848).

### Findings
- **No P1/P2 findings.**
- P3 (non-blocking, cosmetic):
  1. `runpy` RuntimeWarning when launched via `python -m` (established repo pattern; console entry point unaffected).
  2. Module docstring (L8) says "NDJSON diagnostics on stderr" while `_serialize_error` writes the ADR-001 JSON envelope to stdout (L487) — consistent with the repo-wide error-envelope contract; docstring wording could be tightened.
  3. DEV-STORY-022 title/release_notes still say "12 operations" — stale vs the corrected 8-op surface; noted for story closure (non-blocking, close on verified evidence).

### Decision
**APPROVED.** CODEREVIEW-022 Open → Closed; unblock DEV-022 (remove inbound Blocks link); DEV-022 Resolved → Closed.
