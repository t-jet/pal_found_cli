Subject: Review evidence — CORRECTIONS REQUIRED (P1 filters kwarg)
Created: 2026-08-10T12:30:20
Updated: 2026-08-10T12:30:20
---
## Review evidence — CORRECTIONS REQUIRED (CODEREVIEW-017)

Reviewer: tech-lead. HEAD 62c269f (workflow_tuning_checkpoint-01). Mandatory pre-review verification PASSED: implementation files exist (src/foundry_cli/connectivity/scripts/foundry_connectivity_cli.py, metadata-allow-list.md, .claude/skills/foundry-connectivity/), py_compile exit 0, focused suites (connectivity + media_sets + access_control_guard) 140 passed, full suite 1214 passed (86.28% branch).

### Verified correct
- OP_SPECS exactly 20 (Connection 7, FileImport 6, TableImport 6, VirtualTable 1), matching installed SDK 1.102.0 probes (AsyncConnectionClient/AsyncFileImportClient/AsyncTableImportClient/AsyncVirtualTableClient).
- Nested dispatch via client.connectivity.Connection(.FileImport|.TableImport|.VirtualTable) — exact public client paths.
- PAGINATED_OPS = file_import.list/table_import.list ONLY, via with_raw_response + PaginationHelper (--page-size/--page-token/--all/--max-pages). Raw decode() returns ListFileImportsResponse/ListTableImportsResponse with data/next_page_token (verified fields).
- ACL: 13-op write set; get_configuration_batch (POST) semantic read; packaged metadata allow-list 7 PERMITTED / 13 BLOCKED matches policy.
- upload_custom_jdbc_drivers: bounded 16 MiB file read after ACL before client; .jar validation; body appended positionally (SDK signature (connection_rid, body, *, file_name) verified).
- include_attribution=False; B3 via invocation_scope; secrets never echoed.
- Retry/timeout ADR-002 (1..3600); ADR-001 error taxonomy; ADR-004 output; ADR-005 NDJSON.
- pyproject entry points foundry-connectivity (line 40), package-data (54), ruff E402 (84).

### P1 — BLOCKING: file-import create/replace dispatch `filters=` but SDK requires `file_import_filters=`
- src/foundry_cli/connectivity/scripts/foundry_connectivity_cli.py line 186 (file-import create): `required=("dataset_rid", "display_name", "filters", "import_mode")`; line 188: `list_json_args=frozenset({"filters"})`.
- Same file line 225 (file-import replace): `required=("display_name", "filters", "import_mode")`; line 227: `list_json_args=frozenset({"filters"})`.
- `_build_kwargs` (lines 504-527) forwards the spec arg name as the SDK kwarg, so the SDK receives `filters=[...]`.
- Installed SDK 1.102.0 `AsyncFileImportClient.create`/`replace` signatures require `file_import_filters` (verified via inspect and source .venv/Lib/site-packages/foundry_sdk/v2/connectivity/file_import.py lines 67 and 358). `filters=` is not a known kwarg.
- Runtime proof (valid RIDs): calling with `filters=` yields `ValidationError: file_import_filters — Missing required keyword only argument`. These two operations can NEVER succeed; they fail at SDK validation with exit 1 before any network call.
- The unit test tests/test_foundry_connectivity_cli.py line 504 (`test_file_import_create_dispatches_with_filters_json`) asserts `filters=[...]` against a mock, so the suite passes while the real SDK contract is violated.
- Fix: rename the spec arg name `filters` -> `file_import_filters` in both OP_SPECS entries (CLI flag `--filters-json` stays unchanged), and update the dispatch assertion to `file_import_filters=[...]`. Apply the same to file-import replace if its dispatch test asserts `filters=`.

### Decision
Move to Correction, assign to python-developer (DEV-017 implementer). All other review criteria verified clean.
