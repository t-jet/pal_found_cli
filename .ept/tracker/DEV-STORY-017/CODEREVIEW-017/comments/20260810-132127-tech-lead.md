Subject: Re-review evidence — APPROVED (P1 fix verified)
Created: 2026-08-10T13:21:27
Updated: 2026-08-10T13:21:27
---
## Re-review evidence — APPROVED (CODEREVIEW-017)

Reviewer: tech-lead. Re-review of the P1 corrective action documented in 20260810-130004-python-developer.

### P1 fix verified

- `src/foundry_cli/connectivity/scripts/foundry_connectivity_cli.py`: `file_import.create` (L186/L188) and `file_import.replace` (L225/L227) now register `file_import_filters` in `required` and `list_json_args` instead of `filters`.
- `_FLAG_NAME_OVERRIDES` (L311) maps `file_import_filters` to the public flag `filters`, so the CLI surface stays `--filters-json` while `_add_kwarg` (L333) keeps `dest=file_import_filters`. Dispatch paths (`_validate_inputs` L459, `_build_kwargs` L494) forward spec arg names unchanged, so the SDK call receives exactly `file_import_filters=`.
- Installed SDK 1.102.0 contract re-verified via `inspect.signature`: `AsyncFileImportClient.create` and `replace` both require keyword-only `file_import_filters: List[Union[FileFilter...]]`; no `filters` parameter exists. The fix matches the real contract.
- Tests: `test_file_import_create_dispatches_with_filters_json` (L504) now asserts `file_import_filters=`; new `test_file_import_replace_dispatches_with_filters_json` covers replace dispatch. Both pass.

### Verification (independent re-run)

- `compileall` on the corrected source: exit 0.
- `tests/test_foundry_connectivity_cli.py` + `tests/test_access_control_guard.py`: 110 passed, 0 failed.
- Targeted dispatch tests: 2 passed.

### Decision

Correction approved; no further findings. P1 was the only finding in the original review (all other criteria verified clean). Note for the record: the fix is present in the working tree, uncommitted (HEAD 62c269f); commit it at the next checkpoint.

Unblocking DEV-017 by removing LINK-00577 (CODEREVIEW-017 -> DEV-017), then closing CODEREVIEW-017 and DEV-017.
