Subject: Analysis complete: scope, constraints, dependencies
Created: 2026-08-10T10:18:26
Updated: 2026-08-10T10:18:26
---
## Analysis stage: technical scope, constraints, and dependencies

### Technical scope
- Implement the foundry-connectivity Claude skill and CLI exposing exactly 20 SDK v2 connectivity operations: Connection (7), FileImport (6), TableImport (6), VirtualTable (1).
- CLI names kebab-case; OP_SPECS keys/ACL paths snake_case; immutable operation catalog.
- Nested client dispatch: client.connectivity.Connection / Connection.FileImport / Connection.TableImport / Connection.VirtualTable.
- Pagination via PaginationHelper on file_import.list and table_import.list (ResourceIterator with page_size/page_token). Flags: --page-size, --page-token, --all, --max-pages.
- upload_custom_jdbc_drivers: bounded file read (bytes body) before client construction.
- Shared options: --timeout, --format, --pretty.

### Constraints
- FR-ATTR-4 excludes connectivity -> include_attribution=False.
- Metadata-only allow-list packaged per namespace: 7 PERMITTED / 13 BLOCKED (match canonical).
- ACL write set (13): connection.create, connection.update_export_settings, connection.update_secrets, connection.upload_custom_jdbc_drivers, file_import.create/delete/execute/replace, table_import.create/delete/execute/replace, virtual_table.create. get_configuration_batch is POST but semantic read.
- Secret-bearing operations (update_secrets, update_export_settings) must never print secrets; JSON body inputs from --*-json flags.
- B3-only tracing via invocation_scope; SDK-native error handling; exit codes per ADR-001.
- No binary downloads -> BinaryDownloadHandler NOT required for reads.

### Technical components
- src/foundry_cli/connectivity/ (catalog, parser, dispatch, JSON validators, packaged policy).
- .claude/skills/foundry-connectivity/ SKILL.md + scripts launcher.
- pyproject.toml console entry point (foundry-connectivity), package data, ruff E402 scope.
- Tests: tests/test_foundry_connectivity_cli.py (mirror existing namespace test conventions).

### Dependencies
- Shared common components: AccessControlGuard, PaginationHelper, RetryHandler, OutputFormatter, ErrorSerializer, TracingProvider, ConfigLoader, AuthProvider, LogSetup.
- Vendored SDK foundry_sdk/v2/connectivity/.
- No upstream blockers; no QUESTION sub-tasks needed (all contracts resolved from SDK + canonical docs).

### Release notes
- Release notes: Adds foundry-connectivity skill exposing 20 connectivity API v2 operations (Connection, FileImport, TableImport, VirtualTable) for connector source and sync job management, with pagination, ACL, metadata-only policy (7/13), and B3 tracing.

Analysis DoD check: BA-DES-001/SA-DES-001 terminal (Closed) - verified; release_notes populated (next step); technical scope and constraints documented; components and dependencies identified.
