Subject: New → Open DoD satisfied
Created: 2026-07-28T20:07:06
Updated: 2026-07-28T20:07:06
---
## New → Open Transition DoD Review

Addresses the gaps flagged in comment `20260728-162407-manager` (Acceptance Criteria TODO; Related Documentation review missing).

### Documentation reviewed (`.ept/docs/document_index.md`)
- **SAD-001** — EPIC-003 scope; DEV-STORY-008 = `foundry-functions` (7 operations, High). Confirmed 7-operation count against Phase 2 roadmap.
- **SRS-001** — §2.2 Product Functions; §4.2 FR-ACL (8-step access control); **FR-ATTR-4** mandates attribution on `functions.query.*`.
- **Canonical Env Var Reference** — `functions` namespace table: confirmed exactly 7 operation rows (authoritative source for the catalog).
- **Metadata Allow-list** — `functions` namespace: 5 PERMITTED (`query.get`, `query.get_by_rid`, `query.get_by_rid_batch`, `value_type.get`, `version_id.get`), 2 BLOCKED as computation (`query.execute`, `query.streaming_execute`).
- **ADR-001** (exit codes), **ADR-002** (retry/timeout), **ADR-004** (format auto-select), **ADR-005** (NDJSON logging), **ADR-007** (operation-level readonly) — all consulted for AC definitions.

### Acceptance criteria defined
Replaced the TODO with 8 Given/When/Then acceptance criteria (AC-1…AC-8) aligned to: operation catalog completeness, 8-step ACL precedence, metadata-only tier enforcement, FR-ATTR-4 attribution, ADR-001/002 retry+error mapping, ADR-004 format selection, ADR-005 logging/tracing, parser dispatch.

### Technical scope
- 3 SDK classes: `query` (5 ops), `value_type` (1 op), `version_id` (1 op).
- Reuses EPIC-001 common layer (`AccessControlGuard`, `RetryHandler`, `OutputFormatter`, `ErrorSerializer`, `LogSetup`, `TracingProvider`).
- No list → `PaginationHelper` N/A. No binary content → `BinaryDownloadHandler` N/A.
- Deployment: `.claude/skills/foundry-functions/`; CLI entry `src/foundry_cli/functions/scripts/foundry_functions_cli.py`.

### DoD checklist (New → Open)
- [x] Studied related documentation from `.ept/docs/document_index.md`
- [x] Critical thinking applied
- [x] All required questions asked — no QUESTION sub-tasks needed (scope unambiguous; 7 operations verified from two independent authoritative sources)
- [x] Feature Request FEATURE-001 in **Waiting for Implementation** (verified)
- [x] Epic link EPIC-003 identified and present (LINK-00258, EpicLink)
- [x] Technical scope defined (see above)
- [x] Required fields validated: status=New, assignee=architect, priority=High, dates set
- [x] Links to Feature (LINK-00256 Contains, LINK-00257 ParentChild) and Epic (LINK-00258 EpicLink) established

### Blockers
None. Transitioning to Open.

### QUESTION sub-tasks
None created — no ambiguities remain after consulting SRS, SAD, env var reference, and metadata allow-list.

**Note:** EPIC-003 is in `Open` status (no Blocks link to this story), which does not gate pre-Development transitions per the established workflow rule (the epic gates the Development phase, not New/Open/Analysis/Grooming).
