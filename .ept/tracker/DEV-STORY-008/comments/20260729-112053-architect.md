Subject: Analysis complete — technical scope, constraints, dependencies (DoD for Grooming)
Created: 2026-07-29T11:20:53
Updated: 2026-07-29T11:20:53
---
Analysis DoD satisfied for DEV-STORY-008 (foundry-functions skill). Summary of technical scope, constraints, dependencies, and gate checks.

# 1. Technical scope (7 operations, 3 SDK classes)
Namespace 'functions', the smallest EPIC-003 skill (7 ops). Authoritative source: canonical-environment-variable-reference.md and metadata-allow-list.md.
- 'query' class (5 ops): execute, get, get_by_rid, get_by_rid_batch, streaming_execute.
- 'value_type' class (1 op): get.
- 'version_id' class (1 op): get.

Operation-by-operation classification:
- functions.query.execute — ACL BLOCKED under metadata-only (computation); attribution REQUIRED (FR-ATTR-4).
- functions.query.get — PERMITTED; attribution REQUIRED.
- functions.query.get_by_rid — PERMITTED; attribution REQUIRED.
- functions.query.get_by_rid_batch — PERMITTED; attribution REQUIRED.
- functions.query.streaming_execute — ACL BLOCKED under metadata-only (computation); attribution REQUIRED.
- functions.value_type.get — PERMITTED; attribution NOT required.
- functions.version_id.get — PERMITTED; attribution NOT required.

# 2. Constraints
- Access control (AC-2, SRS section 4.2, 8-step model): every op evaluated against global, namespace, and operation controls ('_ENABLED', '_READONLY', '_METADATA_ONLY'); deny returns exit code 8 with the denying rule on stderr (ADR-001).
- Metadata-only tier (AC-3): query.execute and query.streaming_execute are computation ops and are BLOCKED at Tier-3; the 5 descriptor ops are PERMITTED.
- Attribution (AC-4, FR-ATTR-4): attribution RIDs injected into request headers for the 5 functions.query.* operations ONLY when FOUNDRY_AGENTIC_CLI_ENABLE_ATTRIBUTION=true; value_type and version_id are exempt.
- Retry/timeout/error (AC-5, ADR-001/ADR-002): RetryHandler default 4 attempts; final error mapped via ErrorSerializer to ADR-001 taxonomy.
- Output format (AC-6, ADR-004): auto selects JSON for single-resource, TOON for batch (get_by_rid_batch).
- Logging (AC-7, ADR-005): NDJSON structured logs to stderr with B3/W3C propagation when enabled.
- Parser/dispatch (AC-8): entry point foundry_functions_cli.py routes 'functions <class> <operation> [flags]'.

# 3. Components and dependencies
Common-layer reuse (EPIC-001, src/foundry_cli/common/):
- AccessControlGuard, RetryHandler, OutputFormatter, ErrorSerializer, LogSetup, TracingProvider — all reused; no new shared infra.
- NOT required for this namespace: PaginationHelper (no list ops), BinaryDownloadHandler (no binary content).
Reference implementation: DEV-STORY-007 (foundry-ontologies, Closed) — same common-layer pattern to follow.
Deployment target: .claude/skills/foundry-functions/ with CLI at src/foundry_cli/functions/scripts/foundry_functions_cli.py.

# 4. Gate checks (Analysis to Grooming DoD)
- Studied documentation: document_index.md plus SAD-001, SRS-001 (sections 2.2, 4.2, FR-ATTR-4), canonical-environment-variable-reference.md, metadata-allow-list.md, ADR-001, ADR-002, ADR-004, ADR-005, ADR-007. PASS.
- Critical thinking applied: catalog independently re-derived (7 ops, 3 classes); confirmed 5 PERMITTED vs 2 BLOCKED under metadata-only; confirmed attribution scope limited to query.* PASS.
- QUESTION sub-tasks: list --parent DEV-STORY-008 returns 0 children; no open questions. PASS.
- BA-DES-001 status: Closed (terminal). SA-DES-001 status: Closed (terminal). PASS.
- Release notes: field already populated with a meaningful description (7 ops, 3 SDK classes, subprocess CLI, attribution on query.*, metadata-tier blocking of execute/streaming_execute). PASS.
- Technical scope and constraints: documented in this comment. PASS.
- Technical components and dependencies: identified in section 3. PASS.

All DoD criteria met. Transitioning to Grooming.
