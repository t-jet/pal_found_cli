Subject: Test cases defined; deliverable + discrepancies + blocker note
Created: 2026-07-04T22:46:49
Updated: 2026-07-04T22:46:49
---
## TESTCASE-001 — Test Cases Defined (Deliverable)

**Author:** qa-engineer  **Date:** 2026-07-04  **Status:** In Progress → Resolved (requesting review)

### Deliverable Location
Full test-case specification committed to:
`.ept/docs/deliverables/qa/TESTCASE-001-test-cases.md`

### Summary
- **TC1 RetryHandler:** 9 functional cases (backoff, max_delay cap, jitter bounds, env vars FOUNDRY_MAX_RETRIES/RETRY_BASE_DELAY/RETRY_MAX_DELAY/RETRY_JITTER, decorator, max_retries=0, ctx mgr) + 4 regression cases.
- **TC2 ErrorSerializer:** 11 cases (exit codes 1–9 mapping, 503 exclusion, JSON envelope fields, FOUNDRY_INCLUDE_TRACEBACK=false) + 2 regression cases.
- **TC3 OutputFormatter:** 9 cases (json/toon/auto, ADR-004 7-step algorithm, --pretty, format_error) + 5 regression cases.
- **TC4 LogSetup:** 8 cases (NDJSON single-line, required fields ts/level/logger/msg, ISO 8601 UTC, optional context, level filter, default WARNING, unsupported-level ValueError, metadata separator exact) + 2 regression cases.
- **TC5 Integration:** 3 cases (RetryHandler+ErrorSerializer, OutputFormatter+LogSetup stream separation, full pipeline).
- **TC6 Non-Functional:** 5 cases (perf <5ms/<10ms/<10ms, secret-leakage, no handler duplication under concurrency).

### AC Coverage
All DEV-STORY-002 ACs (RetryHandler, ErrorSerializer, OutputFormatter, LogSetup, Integration) are covered by named test case IDs in the coverage matrix.

### Implementation Status Verified
Implementation files DO exist in `src/foundry_cli/common/`: `retry.py` (9.1 KB), `error_serializer.py` (13.5 KB), `output_formatter.py` (7.9 KB), `log_setup.py` (7.5 KB). Test scenarios reference real implemented functions/classes. DoD "MANDATORY: Test scenarios reference actual implemented features" satisfied.

### Discrepancies Flagged for BUG-SUB (do not block this TESTCASE)
1. **TC1.R1** — env var names mismatch: impl uses `FOUNDRY_*`, AC specifies `FOUNDRY_AGENTIC_CLI_RETRY_*`.
2. **TC1.R2** — `asyncio.wait_for()` timeout NOT implemented.
3. **TC1.R3** — no HTTP 429/503-specific retry predicate (default is broad `requests.RequestException`).
4. **TC1.R4** — no SIGINT/SIGTERM signal handling.
5. **TC2.R1** — exit code 8 (AccessControlError) unreachable via `serialize()`.
6. **TC2.R2** — envelope schema differs from AC (missing attempt/operation/details).
7. **TC3.R2** — TOON rendered via hand-rolled table, not `toon-python` library.
8. **TC3.R3** — `OutputFormatter.emit_to_stderr` lacks `# ---metadata-start---` separator.
9. **TC4.R2** — no log-file/rotation (AC vs ADR-005 ambiguity).

QA recommends opening BUG-SUB tickets before TESTEXEC-001 execution. These do not block TESTCASE-001 since DoD only requires that test cases reference real features and cover ACs (which they do, including the gaps as negative/regression cases).

### Blockers
TESTCASE-001 is currently marked Blocked via LINK-00094 (DEV-002 Blocks TESTCASE-001). Since implementation files exist and are testable, this block may be stale; requesting tech-lead/developer confirmation. Will not transition to Resolved until block is resolved OR confirmed stale.

### Time
~3.5h spent (test case design, code review, AC mapping).
