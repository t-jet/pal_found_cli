Subject: DevOps validation complete and DoD evidence
Created: 2026-07-26T22:35:51
Updated: 2026-07-26T22:35:51
---
## DevOps validation evidence

Documentation studied/updated: `.ept/docs/document_index.md` and `README.md`.

Files changed by this DevOps pass: `.ept/docs/document_index.md`, `README.md`. Pre-existing CI/package changes already present: `.github/workflows/ci.yml` Python 3.11/3.12 matrix, pyproject README long_description + foundry-platform-sdk>=1.0.0 + Ruff test ignores, restored `src/foundry_cli/datasets` wrapper package.

Coverage resolution: pyproject enforces 80% repository-wide branch coverage; validation result is 81.65% with 262 tests passing. The old 90% checklist text is documented as aspirational/new-code guidance unless quality standard is formally raised, not silently lowered.

Validation commands and results:
- `python -m ruff check src tests` passed.
- `python -m mypy src` passed, no issues in 14 source files.
- `python -m pytest tests --cov=foundry_cli --cov-report=term-missing --cov-report=xml -q` passed, 262 passed, 81.65%, required 80 reached.
- `python -m bandit -r src --severity-level high` passed, no issues identified.
- `python -m build` passed, built sdist and wheel, README copied into sdist, datasets wrapper included in wheel.
- `python -m twine check dist\\*` passed for wheel and sdist.

Time reported: 4 hours.

Links: no active blocker links visible.
