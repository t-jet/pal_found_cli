Subject: Open DoD evidence and developer fix plan
Created: 2026-07-21T21:22:53
Updated: 2026-07-21T21:22:53
---
## Open DoD evidence and developer fix plan

### Documentation reviewed

Reviewed project docs from `.ept/docs/document_index.md` relevant to DEV-STORY-002 and `src/foundry_cli/common/error_serializer.py`:

- `SRS-001-foundry-cli.md`: FR-ERR-1, FR-ERR-2, FR-ERR-5; NFR-IFACE-1/2.
- `SAD-001-foundry-cli.md`: common module component model and error handling strategy.
- `ADR-001-exit-code-taxonomy.md`: AccessControlError must map to exit code `8`.
- `ADR-005-log-format.md`: diagnostics/log records belong on stderr as NDJSON; structured error envelope belongs on stdout.

### Critical thinking and questions

No QUESTION sub-task is needed. The requirement is explicit in ADR-001 and the defect has a named scenario source: TESTCASE-001 comment `20260704-224649-qa-engineer`, discrepancy `TC2.R1`.

Scope is narrow enough for the existing BUG-SUB. It affects `ErrorSerializer.serialize()` behavior for AccessControlError only, plus regression coverage.

### Unsatisfied requirement / error

Unsatisfied requirement: AccessControlError must serialize as a structured stdout error and map to exit code `8` per ADR-001. Current defect statement says exit code `8` is unreachable through `ErrorSerializer.serialize()`.

### Steps to reproduce

1. In the DEV-STORY-002 test design context, execute or implement TC2.R1 from TESTCASE-001.
2. Exercise `ErrorSerializer.serialize()` with an AccessControlError instance or equivalent access-control exception path.
3. Inspect the serialized error envelope and mapped exit code returned by the serializer path.

### Expected behavior

- `ErrorSerializer.serialize()` accepts/handles AccessControlError.
- Serialized stdout envelope uses `error.type = "AccessControlError"`.
- Message and details are preserved where available.
- Exit code maps to `8`, matching ADR-001.

### Actual behavior

TESTCASE-001 evidence reports TC2.R1 failure condition: `exit code 8 (AccessControlError) unreachable via serialize()`.

### Environment details

- Repository: `E:\learn\GenAI_Foundations_DA\git\foundry_cli`.
- OS/shell observed by QA session: Windows PowerShell.
- Current date: 2026-07-21.
- Target component: `src/foundry_cli/common/error_serializer.py`.
- Parent story: `DEV-STORY-002`.
- Blocking link: `LINK-00153` from `BUG-SUB-004` to `TESTEXEC-001`.
- Evidence source: TESTCASE-001 comment `20260704-224649-qa-engineer`.

### Logs and evidence

No runtime application log was produced in this QA handoff because this is a documented test-design discrepancy and no code/test execution was performed during the handoff. Evidence is the tracker record and cited test-case comment:

- BUG-SUB-004 comment `20260721-201815-qa-engineer`: docs reviewed, parent context reviewed, TESTCASE evidence cited, links confirmed.
- TESTCASE-001 comment `20260704-224649-qa-engineer`: TC2.R1 discrepancy recorded: `exit code 8 (AccessControlError) unreachable via serialize()`.
- BUG-SUB-004 comment `20260721-210638-python-developer`: developer confirmed implementation is blocked while ticket remains Open.

### Fix plan for python-developer

1. Inspect `src/foundry_cli/common/error_serializer.py` for the exception type mapping used by `ErrorSerializer.serialize()` and the exit-code mapper.
2. Add or correct AccessControlError handling so direct serialization produces `error.type = "AccessControlError"` and exit code `8`.
3. Preserve existing envelope fields and message/details behavior for other error categories.
4. Add regression coverage for direct AccessControlError serialization through `serialize()` and exit-code mapping.
5. Run the targeted ErrorSerializer tests, then broader common-module tests if available.
