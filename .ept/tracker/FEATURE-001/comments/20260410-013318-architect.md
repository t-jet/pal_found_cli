Subject: Requirements Completeness Assessment — Ready to Proceed
Created: 2026-04-10T01:33:18
Updated: 2026-04-10T01:33:18
---
## Requirements Completeness Assessment — Ready to Proceed

**Date:** 2026-04-10
**Assessment:** All three rounds of open questions are fully answered. Information is sufficient to write both the Requirements Document and Architecture Design.

---

### Fully Resolved Topics (all decision-complete)

| Domain | Status |
|---|---|
| Deliverable scope — 21 skills, 20 namespaces + 1 general | Resolved |
| Auth — UserTokenAuth, FOUNDRY_TOKEN / FOUNDRY_HOSTNAME | Resolved |
| Attribution & tracing — opt-in, FOUNDRY_AGENTIC_CLI_ prefix | Resolved |
| Output format — Option B: raw TOON/JSON stdout, metadata JSON stderr | Resolved |
| TOON library — accept beta toon-python, >=0.9,<1.0 | Resolved |
| Pagination — first-page-only + batch up to 40 pages | Resolved |
| Error handling — structured JSON + exponential backoff retry | Resolved |
| Binary downloads — UUID folder, 1.5 MB default, partial with truncation warning | Resolved |
| Access control — 8-step precedence model, deny-by-default metadata allow-list | Resolved |
| Env var naming — FOUNDRY_AGENTIC_CLI_ prefix, full-path flattening | Resolved |
| .env loading — .env primary, env vars override | Resolved |
| Session management — named aliases (collision=error), 7-day retention, advisory limit | Resolved |
| Async client — AsyncFoundryClient internally, asyncio.run() at CLI boundary | Resolved |
| Code structure — _foundry_cli_common.py shared module alongside each namespace file | Resolved |
| Skill format — .claude/skills/{foundry-{ns}}/SKILL.md + scripts/ | Resolved |
| General skill — pre-authored static, auto-loaded, 9 doc URLs provided | Resolved |
| Python targets — 3.11.* and 3.12.* | Resolved |
| Deployment — file copy to target repo, Win11/macOS/Linux | Resolved |
| Metadata allow-list — SA deliverable, .env config, deny-by-default | Resolved |

---

### Items to Resolve During Architecture Phase (no further customer input required)

These are implementation-level decisions that the Solution Architect resolves as part of the design work:

| Item | Decision needed |
|---|---|
| Exit code taxonomy | Define specific non-zero codes for error types (user error vs system error vs auth error vs permission error) |
| Operation timeout defaults | Default asyncio.wait_for() timeout value and FOUNDRY_AGENTIC_CLI_TIMEOUT_S env var |
| Streaming namespace handling | How streams namespace operations are handled (batch vs streaming response) |
| --format flag valid values | Enumerate: json / toon / auto; define auto-detection rule |
| Logging format and destination | Structured JSON to stderr; define required log fields |
| .env file search path order | Which .env paths are probed and in which priority order |
| Operation-level READONLY=true | Confirm if independent (not just override) op-level READONLY=true is supported |

---

### Architecture Deliverables Required

Per decisions in Q3(R3).4-C and Q3(R3).8-A, the following must be produced as part of the architecture phase:

1. **Canonical env var reference table** — namespace → class → operation → env var name (500+ entries)
2. **Metadata allow-list configuration** — operation-by-operation classification for all 20 namespaces, delivered as .env config file
3. **Requirements Document** 
4. **Architecture Design Document** (SAD with C4/C5 diagrams)
5. **ADRs** for all major technology and design decisions

---

**Next action:** Create FEATURE tickets for Requirements Document and Architecture Design deliverables.
