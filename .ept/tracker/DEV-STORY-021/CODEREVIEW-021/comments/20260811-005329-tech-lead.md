Subject: Review evidence — APPROVED (2026-08-11 tech-lead): 9-op third-party-applications CLI verified, decision: Closed
Created: 2026-08-11T00:53:29
Updated: 2026-08-11T00:53:29
---
---
## Code Review — CODEREVIEW-021: DEV-021 (foundry-third-party-applications CLI, 9 ops)

**Decision: APPROVED — move to Closed.** No P1/P2 findings. P3 notes only (non-blocking).

### MANDATORY pre-review verification (all PASS)
- Implementation files exist at commit `74094bc` (11 files, incl. `.claude/skills/foundry-third-party-applications/SKILL.md` + thin launcher — skill deliverable shipped in the DEV commit, per design component breakdown).
- `compileall` exit 0 on `src/foundry_cli/third_party_applications/` + tests.
- Launcher `--help` exit 0, lists all 9 operations.
- Focused suite: **118 passed** (`test_foundry_third_party_applications_cli.py` 42, console wrapper 3, ACL regression).
- Full regression: **1318 passed, 0 failed** (matches developer claim).
- ruff: clean. mypy source: 0 errors (the 3 test-file errors — `_Scope.__exit__` bool L37, `_Cfg` arg-type L731/L751 — are the established pre-existing convention seen in every namespace test suite).

### 9-op catalog — exact match vs INSTALLED SDK 1.102.0 (inspect.signature probes)
| # | CLI | SDK dispatch (installed signature) | Match |
|---|---|---|---|
| 1 | third-party-application get | `ThirdPartyApplication.get(third_party_application_rid)` | ✓ |
| 2 | website deploy | `Website.deploy(rid, *, version)` | ✓ |
| 3 | website get | `Website.get(rid)` | ✓ |
| 4 | website undeploy | `Website.undeploy(rid)` | ✓ |
| 5 | version delete | `Version.delete(rid, version_version)` | ✓ |
| 6 | version get | `Version.get(rid, version_version)` | ✓ |
| 7 | version list | `Version.list(rid, *, page_size, page_token)` → ResourceIterator | ✓ |
| 8 | version upload | `Version.upload(rid, body: bytes, *, version)` | ✓ |
| 9 | version upload-snapshot | `Version.upload_snapshot(rid, body, *, version, snapshot_identifier)` | ✓ |

- `preview` correctly excluded from all ops (upload_snapshot has an SDK `preview` param; CLI omits it — per DESIGN-021 "preview parameters are excluded").
- Nested routing `root.third_party_applications.Website.Version` verified against installed SDK (`cached_property Version` present on `WebsiteClient`).
- Dispatch probe: `_build_kwargs` yields exactly the SDK kwarg names (`version`, `request_timeout` only; `file` consumed by CLI, `snapshot_identifier` omitted when absent).

### ACL — including new deploy/undeploy verbs (access_control_guard.py)
- `deploy` + `undeploy` added to `_WRITE_VERBS` (`src/foundry_cli/common/access_control_guard.py` L86-90) with DESIGN-021 rationale comment. Verified: 5-op write set (`website.deploy`, `website.undeploy`, `version.delete`, `version.upload`, `version.upload_snapshot`) classified as writes; 4 reads (`third_party_application.get`, `website.get`, `version.get`, `version.list`) not.
- Read-only mode blocks all 5 writes → exit 8, before file read / client creation (test `test_readonly_blocks_five_write_operations` L678; runtime `main()` L468 ACL → L473 file read → L480 client).
- Metadata-only policy: fail-closed; packaged `metadata-allow-list.md` permits exactly 4 of 9 (`third_party_application.get`, `website.get`, `version.get`, `version.list`) — byte-concordant with canonical allow-list (`.ept/docs/deliverables/architecture/metadata-allow-list.md` L444-456) and with env-var reference L669-673.

### Bounded zip uploads
- `version upload`/`upload-snapshot` read `--file` bounded at 16 MiB (`_UPLOAD_MAX_BYTES`, L55) AFTER the ACL decision and BEFORE client construction (`main()` L473-477); `path.stat()` size check before `read_bytes()`. Missing/oversized file → CLIInputError exit 1, zero client calls (tests L575, L602). `--file` never forwarded; body passed positionally per SDK.

### Pagination (version list)
- `version list` is the only paged op (`PAGINATED_OPS` L47). Uses `with_raw_response` + PaginationHelper (`_paginate_operation` L431-466) with `--page-size`/`--page-token`/`--all`/`--max-pages`; page cursor thread-tested (`test_version_list_uses_raw_response_and_helper` L638, token tok1 propagated).

### B3, attribution, ADR-001/002/004/005
- B3 via `factory.invocation_scope(cfg)` (`main()` L482) — `TracingProvider.scope` inside the context manager, restored on all exits (verified in `async_client_factory.py`).
- `include_attribution=False` on both `invocation_scope` and `create` (test `test_invocation_uses_include_attribution_false` L847); namespace outside FR-ATTR-4.
- ADR-001: error envelope on stdout with exit-code taxonomy; runtime probe: missing positional / missing `--version` → structured JSON envelope exit 1 at the console boundary.
- ADR-002: `_validate_timeout` 1..3600 (L277-281); ADR-004: `--format json|toon|auto` + `--pretty`; ADR-005: LogSetup NDJSON, secrets never echoed (`message` sanitized in `_serialize_error`).

### P3 (non-blocking)
1. mypy test-file errors (L37, L731, L751) — established convention across all namespace suites; not blocking.
2. `_METADATA_VERBS` heuristic in `access_control_guard.py` remains dead code for this namespace (allow-list is authoritative) — pre-existing shared-layer note, not introduced here.
3. `python -m` console probe emits a `runpy` RuntimeWarning (cosmetic; installed entry point `foundry-third-party-applications` does not).

### Conclusion
All 9 ops dispatch to exact installed SDK signatures; ACL includes the new deploy/undeploy write verbs; metadata policy 4/9 fail-closed and concordant; pagination and bounded uploads implemented per contract; B3/no-attribution/ADR compliance verified. APPROVED.
---
