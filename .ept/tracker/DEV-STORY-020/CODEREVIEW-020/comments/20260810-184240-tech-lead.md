Subject: Code review findings — DEV-020 foundry-data-health CLI (Decision: Correction)
Created: 2026-08-10T18:42:40
Updated: 2026-08-10T18:42:40
---
## Pre-review verification (passed)

- Implementation files physically exist: `src/foundry_cli/data_health/__init__.py`, `src/foundry_cli/data_health/scripts/__init__.py`, `src/foundry_cli/data_health/scripts/foundry_data_health_cli.py`, `src/foundry_cli/data_health/metadata-allow-list.md` (verified via file search and git ls-tree b0df380).
- Code compiles: `python -m compileall -q src/foundry_cli/data_health` exit 0.
- ruff clean, mypy 0 errors on source.
- Tests: focused suites 52 passed; full suite 1267 passed, 0 failed. Branch coverage: data-health 90%.
- Installed SDK 1.102.0 signature probe: `CheckClient.create/delete/get/replace` and `CheckReportClient.get/get_latest` kwarg names and required markers match OP_SPECS exactly. `Check.CheckReport` nested accessor is a cached_property on installed SDK; root client exposes `data_health.Check` (verified via inspect).
- Console boundary probes: missing token → JSON envelope exit 9; missing required arg → exit 1; ConnectionError → generic message, exit 6.

## Findings

### P1 — Missing skill deliverable (REQUIRES CORRECTION)

`.claude/skills/foundry-data-health/` does not exist — no SKILL.md, no launcher script. Evidence:
- `git ls-tree -r --name-only b0df380` returns 11 files; none under `.claude/skills/`.
- DESIGN-020 Component breakdown lists "Claude skill and launcher for foundry-data-health" as a deliverable.
- Story title is "foundry-data-health skill".
- Every prior namespace ships `.claude/skills/<ns>/SKILL.md` + `scripts/<ns>_cli.py` thin launcher in its DEV commit (e.g., commit 62c269f for connectivity/media-sets).
- Required action: add `.claude/skills/foundry-data-health/SKILL.md` documenting the 6 operations (check create/delete/get/replace; check-report get/get-latest), `--config-json` flag for CheckConfig/ReplaceCheckConfig discriminated union, `--limit` on get-latest, ACL write set (create/delete/replace) and metadata-only policy (3 permitted: check.get, check_report.get, check_report.get_latest; 3 blocked), include_attribution=False, at-least-once retry disclosure, plus thin launcher `scripts/foundry_data_health_cli.py` re-exporting console_main (mirror connectivity launcher pattern).

### P3 — `--limit` bounds not locally validated (non-blocking)

`src/foundry_cli/data_health/scripts/foundry_data_health_cli.py` L19-20 docstring and DESIGN-020 state `--limit` "default 10, maximum 100". `_INT_ARG_NAMES={"limit"}` (L178) applies only `type=int`; no 1..100 bound check. Installed SDK `CheckReportLimit` is a plain `int` alias (no Annotated bounds; probed — SDK accepts limit=0 and limit=150). Server enforces and returns 400 → BadRequestError → exit 1 (graceful). Suggested follow-up: add local bound check `1 <= limit <= 100` raising CLIInputError, or document server-side enforcement. Not blocking.

## Verified clean (no action)

- Catalog: exactly 6 ops (`OP_SPECS` L72-108) with nested `Check.CheckReport` client_path for check_report ops, matching installed SDK 1.102.0 (probed).
- `config` decoded dict (L239-245) for CheckConfig/ReplaceCheckConfig discriminated unions.
- Write set create/delete/replace correctly write-classified (runtime probe under READONLY and METADATA_ONLY: all three BLOCKED exit 8; reads PERMITTED). `replace` inherits write classification from shared `_WRITE_VERBS`.
- No pagination flags anywhere; `get_latest --limit` is a single-response bound, not a cursor (test L195-205).
- Attribution: include_attribution=False on both invocation_scope and create.
- B3 tracing via AsyncClientFactory.invocation_scope → TracingProvider.scope.
- Retry/timeout: RetryHandler(timeout_s=) + ADR-002 bounds; at-least-once behavior disclosed in module docstring (L30-33).
- Error serialization ADR-001 privacy-safe; output ADR-004; NDJSON stderr logging ADR-005 via LogSetup.
- pyproject: entry point `foundry-data-health`, package-data `metadata-allow-list.md`, ruff E402 scope present.

## Decision

Correction required (single P1: missing `.claude/skills/foundry-data-health/` deliverable). P3 noted as follow-up only. Code itself is approved-quality.
