Subject: Technical scope defined — foundry-data-health skill (New → Open)
Created: 2026-08-10T17:02:35
Updated: 2026-08-10T17:02:35
---
## Technical scope — DEV-STORY-020 (foundry-data-health)

### Operation catalog — cross-validated from THREE sources (2026-08-10)
Source 1 — vendored SDK `foundry_sdk/v2/data_health/` (v1.102.0): `Check` client exposes **4** public methods (`create`, `delete`, `get`, `replace`) plus nested `CheckReport` client with **2** methods (`get`, `get_latest`) = **6 total**.
Source 2 — canonical env-var reference: namespace `data_health`, exactly 6 operation rows (`..._CHECK_CREATE_/DELETE_/GET_/REPLACE_`, `..._CHECK_REPORT_GET_/GET_LATEST_`).
Source 3 — metadata allow-list: namespace `data_health`, 6 rows — 3 PERMITTED (`check.get`, `check_report.get`, `check_report.get_latest`), 3 BLOCKED (`check.create`, `check.delete`, `check.replace`).

### Conclusion
Title count "4 operations" is **STALE** — actual SDK contract is **6 operations** (Check 4 + CheckReport 2). Corrected in scope (same precedent: streams 17→15, connectivity 15→20). SAD-001 EPIC-007 row also says "4 operations" — stale; corrected here and in DESIGN-020.

| # | CLI command | SDK dispatch | HTTP and result |
|---|---|---|---|
| 1 | `check create` | `client.data_health.Check.create` | `POST /v2/dataHealth/checks`; `Check` |
| 2 | `check delete` | `client.data_health.Check.delete` | `DELETE /v2/dataHealth/checks/{checkRid}`; None |
| 3 | `check get` | `client.data_health.Check.get` | `GET /v2/dataHealth/checks/{checkRid}`; `Check` |
| 4 | `check replace` | `client.data_health.Check.replace` | `PUT /v2/dataHealth/checks/{checkRid}`; `Check` |
| 5 | `check-report get` | `client.data_health.Check.CheckReport.get` | `GET /v2/dataHealth/checks/{checkRid}/checkReports/{checkReportRid}`; `CheckReport` |
| 6 | `check-report get-latest` | `client.data_health.Check.CheckReport.get_latest` | `GET /v2/dataHealth/checks/{checkRid}/checkReports/getLatest`; `GetLatestCheckReportsResponse` |

### Key technical facts
- Write set = `check.create`, `check.delete`, `check.replace` (3 writes). Verbs `create`/`delete` already in `AccessControlGuard`; `replace` inherits the replace-class write classification (same as `file_import.replace`/`table_import.replace` in connectivity).
- Reads = `check.get`, `check_report.get`, `check_report.get_latest` (3).
- No pagination: `get_latest` takes `--limit` (int, default 10, max 100) — a bounded list, NOT a cursor. No `ResourceIterator`. PaginationHelper NOT required.
- No binary downloads/uploads.
- Attribution: **not** in FR-ATTR-4 scope → `include_attribution=False`.
- Required inputs: `--config-json` (create/replace, with `--intent` optional), `check_rid` (+ `check_report_rid` for report get), `--limit` optional for get-latest.
- DoD evidence: documentation studied (document_index, SAD-001, SRS-001, ADR-001/002/004/005/006/007, canonical env-var reference, metadata allow-list, DESIGN-005/011/012/013/015/017 patterns); critical thinking applied; no open QUESTIONS (0 non-terminal); FEATURE-001 in Waiting for Implementation; EpicLink EPIC-007 present; fields valid (status New, assignee architect, priority High, dates valid); links FEATURE-001 (Contains + ParentChild LINK-00292/293) and EPIC-007 (LINK-00294) established; RESOURCE-REQ-006 cleared (LINK-00384).
New DoD met → transition to Open.
