Subject: Deployment and packaging evidence (In Progress)
Created: 2026-07-29T23:50:15
Updated: 2026-07-29T23:50:15
---
## Deployment and packaging verification evidence (In Progress)

Real terminal output captured 2026-07-29 in the test environment (Windows, Python install at D:\app\Python). No phantom work — every command below was executed.

### AC1 — package deploy (editable install)
- `python -m pip install -e .` → **exit 0**.
- Result: `Building editable for foundry-cli (pyproject.toml) ... done`; `Installing collected packages: foundry-cli`; `Successfully installed foundry-cli-0.1.0`.
- Editable wheel `foundry_cli-0.1.0-0.editable-py3-none-any.whl` (sha256 `049126ab637148145d7e345af407178ef133fa41684a965406a0547d4cc54402`).

### AC2 — console script smoke
- `foundry-admin --help` → **exit 0**.
- Banner: `Foundry Admin CLI - 66 operations`.
- Subparsers (22): authentication-provider, cbac-banner, cbac-marking-restrictions, enrollment, enrollment-role-assignment, group, group-member, group-membership, group-membership-expiration-policy, group-provider-info, host, marking, marking-category, marking-member, marking-role-assignment, organization, organization-guest-member, organization-role-assignment, role, user, user-provider-info (+ the implicit `-h`).
- Note: a non-fatal `RequestsDependencyWarning` (urllib3 2.3.0 / chardet 7.4.0.post2 / charset_normalizer 3.4.1) prints to stderr; does not affect install/help (consistent with DEVOPS-005/007 evidence).

### AC3 — module smoke
- `python -m foundry_cli.admin.scripts.foundry_admin_cli --help` → **exit 0**, same parser help and `66 operations` banner as the console script.

### AC4 — programmatic artifact audit
- `python -c "import ... foundry_admin_cli as m; ..."` → `OP_SPECS= 66`, `RESOURCES= 21`, `console_main= True`, `async main= True`.

### AC5 — skill package ( `.claude/skills/foundry-admin/` )
- `Test-Path SKILL.md` → **True**; frontmatter `name: foundry-admin`.
- `Test-Path scripts/foundry_admin_cli.py` (launcher) → **True**.

### AC6 — console-script registration
- `importlib.metadata` entry_points(group='console_scripts') → `foundry-admin = foundry_cli.admin.scripts.foundry_admin_cli:console_main` (matches `pyproject.toml [project.scripts]`).
- Module file `src\foundry_cli\admin\scripts\foundry_admin_cli.py` present.

### AC7 — packaging config (pyproject.toml)
- build-system `setuptools>=68` + `wheel`; `requires-python = ">=3.11"`.
- deps: `foundry-platform-sdk>=1.0.0`, `python-dotenv>=1.0.0`, `requests>=2.31.0`.
- tooling: `[tool.mypy]` py3.11/check_untyped_defs; `[tool.ruff.lint]` E,F (E501 ignored), per-file-ignores includes `src/foundry_cli/admin/scripts/*.py` E402; `[tool.coverage.run]` branch + `fail_under = 80`, source `foundry_cli`.

### Acceptance criteria status
| AC | Result |
|---|---|
| AC1 editable install | ✅ exit 0 |
| AC2 console `--help` | ✅ exit 0, 66 ops |
| AC3 module `--help` | ✅ exit 0 |
| AC4 OP_SPECS=66, console_main | ✅ |
| AC5 skill SKILL.md + launcher | ✅ |
| AC6 console_scripts registration | ✅ |
| AC7 packaging config | ✅ |

### Deferred (gated on TESTEXEC-009 / DependsOn LINK-00360)
Full regression suite + coverage % + CI matrix (Ubuntu/Windows, py 3.11/3.12) evidence is downstream of TESTEXEC-009 reaching terminal status (DEV-STORY-009 QA→Deployment gate). The packaging/entry-point/skill package verification above is complete and independent of QA. No deployment defects found in the verified scope.
