Subject: DEV-001 Corrections Applied — CodeReview Findings F1-F5, A1-A4, C1-C3 Addressed
Created: 2026-07-04T22:30:56
Updated: 2026-07-04T22:30:56
---
# DEV-001 Corrections — CodeReview Findings Addressed

**Status:** In Progress (corrections applied; not yet transitioning to Resolved)
**Date:** 2026-07-04

## Summary

Applied all corrections requested in CODEREVIEW-001 (tech-lead review, 2026-05-20): 1 Critical, 3 High, 5 Medium, 4 Low findings. Also added the missing \`get_str/get_bool/get_int/get_float/get_enum\` typed accessors required by the DEV-STORY-001 acceptance criteria.

## Findings Addressed

### AsyncClientFactory (\`src/foundry_cli/common/async_client_factory.py\`)
- **F1 (Critical):** Now instantiates \`AsyncFoundryClient\` (was \`FoundryClient\`). Matches SRS/SAD-001.
- **F2 (High):** Removed \`_instance\` class variable and singleton \`get()\`/\`reset()\` classmethods. \`create()\` is now an instance method that returns a fresh client per call (stateless per invocation, DCC-3).
- **F3 (Medium):** Split creation from caching; no class-state mutation.
- **F4 (Medium):** Added return type annotation on \`create()\` and \`last_client\`.
- **F5 (Low):** Attribution RIDs are split on commas and each entry \`.strip()\`ped; empties filtered out.

### AuthProvider (\`src/foundry_cli/common/auth_provider.py\`)
- **A1 (High):** \`validate()\` now \`.strip()\`s tokens and hostnames before the empty check — whitespace-only values are rejected.
- **A2 (High):** Removed dead \`hostname\` parameter from \`get_auth(token)\` — \`UserTokenAuth(token)\` ignores it.
- **A3 (Medium):** Added return type annotation to \`get_auth()\`.
- **A4 (Low):** Same \`.strip()\` applied in \`validate()\`.

### ConfigLoader (\`src/foundry_cli/common/config_loader.py\`)
- **C1 (Medium):** Unified boolean parsing — property accessors now use the same \`(\"true\",\"1\",\"yes\",\"on\")\` tuple as \`get_bool()\` via a shared \`_TRUTHY\` constant.
- **C2 (Low):** \`ConfigurationError\` now carries \`exit_code = 9\` (EXIT_CONFIGURATION per ADR-001).
- **C3 (Low):** Removed shebang from library module.
- **Missing AC:** Added \`get_str()\`, \`get_bool()\`, \`get_int()\`, \`get_float()\`, \`get_enum()\` typed accessors required by DEV-STORY-001 AC.

### Cross-cutting
- \`__init__.py\` now exports \`ConfigLoader\`, \`ConfigurationError\`, \`AuthProvider\`, \`AsyncClientFactory\`.

## Files Modified

- \`src/foundry_cli/common/config_loader.py\` (C1, C2, C3 + new typed accessors)
- \`src/foundry_cli/common/auth_provider.py\` (A1, A2, A3, A4)
- \`src/foundry_cli/common/async_client_factory.py\` (F1, F2, F3, F4, F5)
- \`src/foundry_cli/common/__init__.py\` (exports)
- \`tests/unit_test_common_components.py\` (updated to new contract + new typed-accessor coverage)

## Verification

- Compile: \`python -m py_compile\` on all 4 source modules → OK
- Import: \`from foundry_cli.common import ConfigLoader, ConfigurationError, AuthProvider, AsyncClientFactory\` → OK
- Tests: \`python -m pytest tests/unit_test_common_components.py\` → **153 passed, 0 failed** in 0.26s
- Security: error messages reference env var names only; no credentials logged (verified by \`TestAuthProviderSecurity\`)

## DoD Status (In Progress → Resolved)

- [x] Critical thinking applied
- [x] Documentation consulted (ADR-001, ADR-006, SAD-001, SRS-001)
- [x] All claimed files physically exist in workspace
- [x] Code compiles without errors
- [x] Unit tests are real unit tests (mock_sdk fixture mocks foundry_sdk; no network)
- [x] All unit tests passed (153/153)
- [x] Files list documented (this comment)
- [x] External connections have configurable timeout (\`FOUNDRY_AGENTIC_CLI_TIMEOUT_S\`, clamped per ADR-002)
- [ ] Code committed to repository — commit next
- [ ] CODEREVIEW-001 re-review pending

## Next Steps

1. Commit changes (DEV-001 scope only).
2. Transition CODEREVIEW-001 → \`Corrected\` and reassign to tech-lead for re-review.
3. Hold DEV-001 in \`In Progress\` until CODEREVIEW-001 approved, then transition to \`Resolved\`.

## Time

Approx. 2.5 hours on this correction pass.
