Subject: Technical scope defined — foundry-checkpoints skill (New → Open)
Created: 2026-08-10T17:02:27
Updated: 2026-08-10T17:02:27
---
## Technical scope — DEV-STORY-019 (foundry-checkpoints)

### Operation catalog — cross-validated from THREE sources (2026-08-10)
Source 1 — vendored SDK `foundry_sdk/v2/checkpoints/` (v1.102.0): `Record` client exposes exactly **3** public methods: `get`, `get_batch`, `search`.
Source 2 — canonical env-var reference: namespace `checkpoints`, exactly 3 operation rows (`FOUNDRY_AGENTIC_CLI_CHECKPOINTS_RECORD_GET_ENABLED` / `_GET_BATCH_` / `_SEARCH_`).
Source 3 — metadata allow-list: namespace `checkpoints`, 3 rows, **all PERMITTED** (tier 3 permits every op: `record.get`, `record.get_batch`, `record.search`).

### Conclusion
Title count "3 operations" is **CONFIRMED** — no correction needed (contrast DEV-STORY-016/017/020 where counts were stale).

| # | CLI command | SDK dispatch | HTTP and result |
|---|---|---|---|
| 1 | `record get` | `client.checkpoints.Record.get` | `GET /v2/checkpoints/records/{recordRid}`; `Record` |
| 2 | `record get-batch` | `client.checkpoints.Record.get_batch` | `POST /v2/checkpoints/records/getBatch`; `GetRecordsBatchResponse` |
| 3 | `record search` | `client.checkpoints.Record.search` | `POST /v2/checkpoints/records/search`; `SearchCheckpointRecordsResponse` |

### Key technical facts
- All 3 ops are **semantic reads** (no writes in the namespace). `get_batch`/`search` are POST-but-read, like prior `search`-style operations.
- `record search` returns `next_page_token` + `data` → **PaginationHelper** with `--page-size`/`--page-token`/`--all`/`--max-pages` (single paged op).
- No binary downloads, no uploads, no sub-nested clients (single `Record` path under `CheckpointsClient`).
- Attribution: **not** in FR-ATTR-4 scope → `include_attribution=False`.
- Required inputs: `record_rid` (get), body JSON list of `recordRid` elements (get-batch, max 100), `--where-json` + optional `--page-size`/`--page-token`/`--sort-direction` (search).
- DoD evidence: documentation studied (document_index, SAD-001, SRS-001, ADR-001/002/004/005/006/007, canonical env-var reference, metadata allow-list, DESIGN-005/011/012/013/015 patterns); critical thinking applied; no open QUESTIONS (0 non-terminal); FEATURE-001 in Waiting for Implementation; EpicLink EPIC-007 present; fields valid (status New, assignee architect, priority High, dates valid); links FEATURE-001 (Contains + ParentChild LINK-00289/290) and EPIC-007 (LINK-00291) established; RESOURCE-REQ-006 cleared (LINK-00382).
New DoD met → transition to Open.
