Subject: Code review findings — DEV-019 foundry-checkpoints CLI (Decision: Correction)
Created: 2026-08-10T18:42:02
Updated: 2026-08-10T18:42:02
---
## Pre-review verification (passed)

- Implementation files physically exist: `src/foundry_cli/checkpoints/__init__.py`, `src/foundry_cli/checkpoints/scripts/__init__.py`, `src/foundry_cli/checkpoints/scripts/foundry_checkpoints_cli.py`, `src/foundry_cli/checkpoints/metadata-allow-list.md` (verified via file search and git ls-tree b0df380).
- Code compiles: `python -m compileall -q src/foundry_cli/checkpoints` exit 0.
- ruff clean, mypy 0 errors on source.
- Tests: focused suites 52 passed; full suite 1267 passed, 0 failed. Branch coverage: checkpoints 88% (data-health 90%), above 80% threshold.
- Installed SDK 1.102.0 signature probe: `RecordClient.get/get_batch/search` kwarg names and required markers match OP_SPECS exactly. Root client exposes `checkpoints.Record` (verified via inspect on installed SDK).
- Console boundary probes: missing token → JSON envelope exit 9; missing operation → exit 1; missing required arg → exit 1. All structured JSON on stdout.

## Findings

### P1 — Missing skill deliverable (REQUIRES CORRECTION)

`.claude/skills/foundry-checkpoints/` does not exist — no SKILL.md, no launcher script. Evidence:
- `git ls-tree -r --name-only b0df380` returns 11 files; none under `.claude/skills/`.
- DESIGN-019 Component breakdown lists "Claude skill and launcher for foundry-checkpoints" as a deliverable.
- Story title is "foundry-checkpoints skill (3 operations)".
- Every prior namespace ships `.claude/skills/<ns>/SKILL.md` + `scripts/<ns>_cli.py` thin launcher in its DEV commit (e.g., commit 62c269f for connectivity/media-sets: `.claude/skills/foundry-connectivity/SKILL.md`, `.claude/skills/foundry-connectivity/scripts/foundry_connectivity_cli.py`).
- Required action: add `.claude/skills/foundry-checkpoints/SKILL.md` documenting the 3 operations (record get / get-batch / search), `--records-json`/`--where-json` flags, pagination flags on record search only, ACL read classification (all 3 reads; metadata-only permits 3/3), include_attribution=False, plus thin launcher `scripts/foundry_checkpoints_cli.py` re-exporting console_main (mirror connectivity launcher pattern).

### P3 — Review-request test count inaccurate (non-blocking)

Comment 20260810-181539 claims "27 tests"; actual count is 25 `def test_*` in tests/test_foundry_checkpoints_cli.py. Cosmetic only.

## Verified clean (no action)

- Catalog: exactly 3 ops (`OP_SPECS` L73-89), matches installed SDK 1.102.0 signatures (probed).
- `get_batch` body dispatched positionally (L316-319) matching SDK `get_batch(self, body, ...)`.
- `record search` sole paginated op (`PAGINATED_OPS` L37; parser L168-173); `where` kwarg routed into pagination extra_kwargs (L385-390); `--page-size`/`--page-token`/`--all`/`--max-pages` present only on search (test L185-208).
- ACL: AccessControlGuard CHECKPOINTS before client construction (L509-513); all 3 read-classified (runtime probe under READONLY and METADATA_ONLY: all PERMITTED).
- Attribution: include_attribution=False on both invocation_scope and create (L511-513).
- B3 tracing via AsyncClientFactory.invocation_scope → TracingProvider.scope.
- Retry/timeout: RetryHandler(timeout_s=) + ADR-002 bounds 1..3600 validated (L336-339).
- Error serialization ADR-001 with privacy-safe messages (probe: ConnectionError → generic "Checkpoints operation failed", exit 6; no values echoed in errors).
- Output ADR-004 (json/toon/auto), NDJSON stderr logging ADR-005 via LogSetup.
- pyproject: entry point `foundry-checkpoints` (L48), package-data `metadata-allow-list.md` (L59), ruff E402 scope (L77).

## Decision

Correction required (single P1: missing `.claude/skills/foundry-checkpoints/` deliverable). P3 noted as follow-up only. Code itself is approved-quality.
