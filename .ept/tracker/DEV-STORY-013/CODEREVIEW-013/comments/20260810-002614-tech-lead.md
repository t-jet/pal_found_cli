Subject: CODEREVIEW-013 review evidence — APPROVED (DEV-013)
Created: 2026-08-10T00:26:14
Updated: 2026-08-10T00:26:14
---
## Pre-review verification (MANDATORY per codereview DoD)

- **Files exist**: `src/foundry_cli/models/scripts/foundry_models_cli.py`, `src/foundry_cli/models/metadata-allow-list.md`, `src/foundry_cli/models/__init__.py`, `.claude/skills/foundry-models/SKILL.md` + `scripts/foundry_models_cli.py`, `tests/test_foundry_models_cli.py`, `pyproject.toml` — all verified via file search (2026-08-10).
- **Compile**: `python -m compileall -q src/foundry_cli/models` → exit 0 (2026-08-10).
- **Smoke test**: `foundry-models --help` → exit 0, lists all 10 resources.
- **Tests**: `pytest tests/test_foundry_models_cli.py` → 33 passed; branch coverage 89% (≥80% gate). Full suite 1024 other tests pass (no regression).
- **Lint/type**: `ruff check` clean; `mypy` on the CLI → no issues.
- **Commit**: all files committed at `bd13955`; no uncommitted changes (verified `git status`).

## Findings (file:line with code snippets)

### P1 — None (no blocking findings)

### P2 — Minor: dead `_is_metadata_operation` in shared ACL guard (pre-existing, not DEV-013-specific)
- `src/foundry_cli/common/access_control_guard.py:545` — `def _is_metadata_operation(self, resource, operation)` defined but never called anywhere (grep confirms only the definition site). `_METADATA_VERBS` frozenset at line 80 is likewise unused. The check() path uses `_is_in_metadata_allowlist` (line 345 area). Harmless YAGNI artifact; no behavioral impact. Recommend cleanup in a future shared-layer ticket, not a correction blocker.

### P3 — Informational: design naming vs actual SDK client path
- DESIGN-013 rows 22-23 name the dispatch `client.models.ModelStudio.Trainer`; the vendored SDK (`foundry_sdk/v2/models/_client.py:77-81`, `113`) exposes `ModelStudioTrainer` as a **top-level** client. Implementation correctly uses `("ModelStudioTrainer",)` at `foundry_models_cli.py:251,257`. Implementation matches the authoritative SDK; design table is cosmetic-only.

## Verified-correct checklist (all confirmed with file:line + SDK signature probes)

- **23-op catalog**: `foundry_models_cli.py:62-267` — exactly 23 unique (resource, operation) pairs; test `test_catalog_contains_exact_23_operations` asserts exact dispatch tuples.
- **Nested dispatch**: `_get_client` (`:298`) resolves `root_client.models.<path>`; all client paths verified against vendored SDK `_client.py` and leaf modules (Model.Version, Model.Experiment, Model.Experiment.Series/ArtifactTable, ModelStudio.ConfigVersion/Run, ModelStudioTrainer).
- **JSON validation before client**: `_validate_inputs` (`:397`) decodes `*_json` args before `AccessControlGuard`/client; invalid JSON → exit 1 via structured envelope, no client creation (test `test_invalid_json_returns_exit_one_without_side_effects`).
- **Pagination scope**: `PAGINATED_OPS` (`:71-79`) = 4 ops; `_paginate_operation` uses `with_raw_response` + `PaginationHelper` (`:528-562`), `HARD_MAX_BATCH_PAGES` cap; trainer list no pagination flags; series/artifact `--offset/--page-size` forwarded once via `_invoke` (test `test_series_json_forwards_slicing_once_not_pagination`).
- **Streaming downloads**: `DOWNLOAD_OPS` (`:81-86`) = series parquet + artifact json/parquet; `_download_operation` (`:566`) uses `with_streaming_response` + `BinaryDownloadHandler.save` with atomic temp+`os.replace`, bounded memory, `aclose` on all paths (verified `AsyncStreamingContextManager.__aexit__` closes; `_close_stream` handles `aclose`).
- **ACL**: `AccessControlGuard(cfg, "MODELS", ...)` before client (`:667-670`); write set includes transform_json/creates/promote_version/launch — `_WRITE_VERBS` (`access_control_guard.py:33-66`) now includes `launch`/`promote`/`pause`/`unpause`. `experiment search` semantic read (test `test_experiment_search_is_semantic_read`). ACL denial → exit 8 + denying rule (test `test_acl_denial_reports_rule_on_stderr`).
- **Metadata-only policy**: packaged `metadata-allow-list.md` 12 PERMITTED / 11 BLOCKED, byte-identical to canonical allow-list; parsed by `_load_metadata_allowlist`; tests `test_metadata_only_permits_exactly_12`/`test_metadata_only_blocks_remaining_11`.
- **B3 tracing / no attribution**: `invocation_scope(cfg, include_attribution=False)` + `factory.create(..., include_attribution=False)` (`:671-674`); test asserts both scope and create kwargs.
- **Retry/timeout ADR-002**: `RetryHandler(timeout_s=timeout)`; `_validate_timeout` 1..3600 (`:385-390`); retry only 429/503 + transport/timeout (`retry.py:_should_retry`); at-least-once disclosure documented in SKILL.md.
- **Error serialization ADR-001**: `_serialize_error` (`:606`) maps SDK errors → exit codes 1-9; `AccessControlError` handled via `except` → `ErrorSerializer().serialize` + exit 8; no secrets leaked (test `test_no_secrets_in_errors_or_tracebacks`).
- **Output/logging**: `OutputFormatter` JSON/TOON auto per ADR-004; NDJSON stderr via `LogSetup`; pagination metadata via `helper.emit_metadata()`.
- **pyproject**: entry point `foundry-models` (`pyproject.toml:36`), package-data `foundry_cli.models = ["metadata-allow-list.md"]` (`:46`), ruff E402 per-file-ignore (`:72`); coverage omit list correctly does NOT omit models (gate applies).
- **Skill packaging**: thin launcher re-exports `build_parser`/`console_main`/`main`; SKILL.md documents catalog, paging, downloads, ACL, no-attribution.

## Decision

**APPROVED.** No corrections required. P2/P3 are non-blocking notes (shared-layer dead code and cosmetic design naming). DEV-013 implementation meets all 10 acceptance criteria and DESIGN-013 contracts.

Also note: I am a Tech Lead agent; you must follow this rule too: you may run at most 2 subagents in parallel at any time.
