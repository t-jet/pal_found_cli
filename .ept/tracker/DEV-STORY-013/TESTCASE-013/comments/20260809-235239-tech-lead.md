Subject: Tech lead approval
Created: 2026-08-09T23:52:39
Updated: 2026-08-09T23:52:39
---
## Tech lead approval

Reviewed `.ept/docs/deliverables/qa/TESTCASE-013-test-cases.md` (28 cases, MDL-TC-001..028) against the actual implementation in the current workspace (commit `bd13955` for the implementation; the deliverable file is the current uncommitted workspace state).

**Verified against `src/foundry_cli/models/scripts/foundry_models_cli.py` (23 `OP_SPECS`):**
- Exactly 23 operations (`live_deployment` 1, `model` 3, `model_version` 3, `experiment` 2, `experiment_series` 2, `experiment_artifact_table` 2, `model_studio` 3, `model_studio_config_version` 4, `model_studio_run` 1, `model_studio_trainer` 2); every MDL-TC-001..028 case references only real commands/routes.
- Exactly 4 cursor-paged ops (`experiment search`, `model-version list`, `model-studio-config-version list`, `model-studio-run list`) via `PaginationHelper` with a 40-page cap; 3 streamed downloads via `BinaryDownloadHandler`; trainer list has no pagination flags.
- Nested client paths match the vendored SDK `_client.py` (including `ModelStudioTrainer` as a top-level client).

**Verified write-set correction (MDL-TC-015):** exactly 7 write commands — `transform_json` + 4 creates (`model`, `model-version`, `model-studio`, `model-studio-config-version`) + `promote_version` + `launch`. `AccessControlGuard._WRITE_VERBS` includes `launch`/`promote`/`pause`/`unpause`, so MDL-TC-016's write-classification regression claim holds.

**Verified permitted-set claim (MDL-TC-017):** packaged `src/foundry_cli/models/metadata-allow-list.md` is exactly 12 PERMITTED / 11 BLOCKED (including `model-studio get`), matching the canonical architecture allow-list row-for-row.

**Shared components:** `AccessControlGuard` 8-step precedence and fail-closed metadata tier, `PaginationHelper` cursor-local retry, `BinaryDownloadHandler` bounded one-probe-byte atomic persistence and unsafe-name rejection, `include_attribution=False` on both client creation and `invocation_scope`, NDJSON stderr (ADR-005) — all consistent with the case expectations.

**Traceability:** the 28-case set covers all 15 DEV-STORY-013 acceptance criteria and the DESIGN-013 rows (catalog, paging, slicing, downloads, ACL, metadata policy, attribution/B3, retry, error taxonomy, timeout, output, privacy, packaging, regression).

**Verdict:** executable against the implemented 23-operation surface. No mismatches found beyond the two corrections QA already applied (write set 9→7; permitted set 12 incl. `model-studio get`), both confirmed accurate. Minor note only (non-blocking): MDL-TC-002 and the inventory use the shorthand `client.models.ModelStudio.Trainer` while the vendored SDK exposes `ModelStudioTrainer` as a top-level client; the case's fixture of "ten public sub-clients" matches the real SDK, so execution is unaffected.

Approval gate for TESTEXEC-013: PASS.
