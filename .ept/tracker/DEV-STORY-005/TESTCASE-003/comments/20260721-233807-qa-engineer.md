Subject: TESTCASE-003 consolidated test-case specification (deliverable reference + DoD compliance)
Created: 2026-07-21T23:38:07
Updated: 2026-07-21T23:38:07
---
# Consolidated Test-Case Specification — TESTCASE-003

## Implementation under test (verified non-phantom)

- File: `.claude/skills/foundry-datasets/scripts/foundry_datasets_cli.py` (~455 LOC)
- Surface: 33 operations across 5 resource clients (Dataset 11, Branch 5, File 5, Transaction 6, View 6)
- Operation count is canonical per architect decision QUESTION-015
  Comment `20260704-233802-architect`. The "26" in DEV-STORY-005 title is stale and to
  be corrected on the parent.
- Testability verification (2026-07-21):
  - `python -c "import foundry_datasets_cli; foundry_datasets_cli.build_parser()"` → OK
  - `pytest tests/test_foundry_datasets_cli.py -q` → **96 passed**
  - Unit suite coverage: 91.91% lines
- Parser sanity (re-validated): `dataset get-schema-batch --dataset-r '["ri.1"]'`,
  `view add-backing-datasets --view-dataset-rid ... --backing-datasets [] --branch main`
  parse correctly.

## Test specification locations

Authoritative references to the per-scenario Given/When/Then specs (one per distinct
behavior path), organized by acceptance-criteria area:

| Area | TC IDs | Maps to AC |
|---|---|---|
| Dataset operations (11) | TC1.1–TC1.13 | AC-TC-1, AC-TC-2 |
| Branch operations (5) | TC2.1–TC2.7 | AC-TC-1, AC-TC-2 |
| File operations (5) | TC3.1–TC3.9 | AC-TC-1, AC-TC-2 |
| Transaction operations (6) | TC4.1–TC4.8 | AC-TC-1, AC-TC-2 |
| View operations (6) | TC5.1–TC5.8 | AC-TC-1, AC-TC-2 |
| Access control (ADR-007) | TC6.1–TC6.8 | AC-TC-4 |
| Retry integration (ADR-002) | TC7.1–TC7.4 | AC-TC-5 |
| Output formatting (ADR-004) | TC8.1–TC8.6 | AC-TC-5 |
| Error handling & exit codes (ADR-001) | TC9.1–TC9.9 | AC-TC-3 |
| Logging / metadata (ADR-005) | TC10.1–TC10.4 | AC-TC-5 |
| Configuration loading (ADR-006) | TC11.1–TC11.3 | AC-TC-5 |
| CLI parser / arg routing | TC12.1–TC12.6 | AC-TC-6 |
| Non-functional (NFR-PLAT/DIST/IFACE) | TC13.1–TC13.4 | AC-TC-2 |
| Discrepancy regressions (D-1..D-9) | cross-refs TC1.7/TC5.7 etc. | AC-TC-9 |

Each TC row has: **Scenario · Input · Expected · Pass Criteria**. Expected results
include exact exit codes from ADR-001 (0–9) and observable side effects (stdout JSON,
stderr NDJSON, mock kwargs). For example:

- **TC1.1** `dataset get` happy path: Input `dataset get <RID>` with mocked Dataset
  client returning a Dataset object → Expected **exit 0, parseable JSON on stdout with
  dataset fields** → Pass: exit code 0 and JSON parses.
- **TC9.7** Generic SDK exception (HTTP 500) → Expected **exit 6** → Pass: exit 6.
- **TC6.1** op-level ENABLED=false on `dataset put-schema` → Expected **exit 8** with an
  `AccessControlError` envelope matching ADR-001 schema → Pass: exit 8 and envelope.

Full per-TC detail lives in the deliverable
`.ept/docs/deliverables/qa/TESTCASE-003-test-cases.md` (recorded for reviewer reference;
authoritative content for this ticket lives in the ACs above and this comment).

## AC ↔ TC traceability summary

- **AC-TC-1** (one scenario per operation, all 33 ops) — covered by TC1.x, TC2.x, TC3.x,
  TC4.x, TC5.x; TC IDs mapped 1:1 to operations.
- **AC-TC-2** (kwargs forwarded, exit 0 happy path) — every operation scenario explicitly
  asserts on the mock client kwargs (e.g. `dataset_rid`, `branch_name`, `page_size`,
  `request_timeout`, `transaction_rid`).
- **AC-TC-3** (ADR-001 negative paths) — TC9.1–TC9.9 enumerate each exit code with the
  exact exception that produces it. Documented deviation D-2 (invalid-JSON args surface
  as exit 6 rather than 1 per ADR-001) is tracked in QUESTION-016 and reflected in the
  expected result of TC1.7 and TC5.7.
- **AC-TC-4** (ADR-007 access control) — TC6.1–TC6.8 cover op-level/namespace/global
  ENABLED, READONLY override, METADATA_ONLY deny, and the `AccessControlError` → exit 8
  path with the ADR-001 envelope.
- **AC-TC-5** (retry/config/output/logging) — TC7.1–TC7.4 (retry + timeout), TC8.1–TC8.6
  (formatting), TC10.1–TC10.4 (logging), TC11.1–TC11.3 (config).
- **AC-TC-6** (phantom-test-case corrective action) — verified by smoke test and 96-test
  unit suite pass; scenarios reference actual functions `build_parser`, `_resolve`,
  `_invoke`, `_get_client`, `_model_to_dict`, `OP_MAP`, and actual operations defined in
  `foundry_datasets_cli.py`.
- **AC-TC-7** (scenarios cover all ACs) — coverage matrix above shows every AC area has
  ≥1 passing scenario design.
- **AC-TC-8** (review and approval) — review coordinated via QUESTION sub-task / approval
  comment (see subsequent entries).
- **AC-TC-9** (discrepancies tracked) — D-1..D-9 each filed as QUESTION-015..QUESTION-023,
  all Closed (terminal). See status matrix in the comment titled
  "QUESTION status matrix for TESTCASE-003 DoD #3 (all linked questions terminal)".

## Discrepancy D-1..D-9 → QUESTION status summary

| D-id | Discrepancy | Filed-as | Status |
|---|---|---|---|
| D-1 | Operation count 26-vs-33 | QUESTION-015 | Closed (architect ruled 33 canonical) |
| D-2 | Invalid-JSON args exit 6 (not 1) | QUESTION-016 | Closed |
| D-3 | `--dataset-r` flag naming | QUESTION-017 | Closed |
| D-4 | PaginationHelper unused / `--batch-pages` ignored | QUESTION-018 | Closed |
| D-5 | file upload no size guard | QUESTION-019 | Closed |
| D-6 | No binary download path / MAX_DOWNLOAD_BYTES | QUESTION-020 | Closed |
| D-7 | OSError errno 11/115 rate-limit heuristic dead code | QUESTION-021 | Closed |
| D-8 | AccessControlError envelope bypasses ErrorSerializer | QUESTION-022 | Closed |
| D-9 | No asyncio.wait_for timeout wrapper | QUESTION-023 | Closed |

All 9 discrepancies closed (terminal) per DoD criterion #3.

## Next step

Request test-case review/approval from tech-lead (per QA-engineer
`<Test_Case_Design>` rule and testcase workflow DoD criterion #9). Tracked in a
separate comment / sub-task entry.
