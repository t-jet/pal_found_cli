Subject: Answer - QUESTION-118 - conda-build tooling prerequisite provided (2026-08-14)
Created: 2026-08-14T14:29:41
Updated: 2026-08-14T14:29:41
---
## Answer (devops-engineer, 2026-08-14)

### Root cause
conda 26.1.1 is installed (Miniforge3 base at d:/app/miniforge3), but the conda-build package was NOT installed. QA evidence .ept/tmp/qa-batch-20260813.md and comment 20260813-190410-qa-engineer both record conda build --version / conda render conda.recipe --json failing with exit 2 because the base installation had no build/render subcommands (conda info reported conda-build version: not installed).

### Environment/tooling prerequisite (provided)
Install conda-build into the base environment, mirroring what .github/workflows/publish.yml already does (conda install --yes conda-build before conda build conda.recipe). Executed on 2026-08-14:

conda install -n base -y conda-build
conda-build 26.7.0 (pyhe63316d_0, conda-forge) installed into d:/app/miniforge3.

### Verification evidence (2026-08-14)
1) conda build --version now exits 0 and prints: conda-build 26.7.0
2) conda render conda.recipe now exits 0 and renders the full recipe metadata (package pal_found_cli 0.1.0, 18 pal-found-* entry points, host and run requirements). Full log: .ept/tmp/conda-render-current.log.

### Note on the exact gate command
QA used conda render conda.recipe --json. In conda-build 26.7.0 the render subcommand does not accept --json (conda render error: unrecognized arguments: --json, exit 2). The plain form conda render conda.recipe is the supported invocation and succeeds (exit 0). If a machine-readable render is required, the equivalent is conda build conda.recipe --json which is supported and also verifies the recipe end to end.

### Conclusion
The runnable conda test-environment gate prerequisite is now met: conda-build provides the build/render tooling, and the recipe renders successfully. Runtime execution evidence (env creation, package build and install, smoke test) is provided on sibling QUESTION-119.
