Subject: QA Open-stage verification
Created: 2026-07-29T13:06:26
Updated: 2026-07-29T13:06:26
---
## QA Open-stage verification

**Author:** qa-engineer
**Timestamp:** 2026-07-29T13:06:26+03:00
**Scope:** TESTCASE-007 for DEV-STORY-006 (`foundry-filesystem` QA test case design)

### Reviewed workflow metadata
- TESTCASE-007 status before work: `Open`; assignee: `qa-engineer`; parent: `DEV-STORY-006`.
- Current allowed transitions: `In Progress`, `Blocked`, `Canceled`, `Rejected`, `Duplicated`.
- Open -> In Progress DoD requires implementation to exist and be accessible, and requires DEV/UNITTEST completion with real code.
- Testcase completion path is `In Progress -> Resolved -> Closed`; testcase has no `Done` status.

### Reviewed project/design context
- `.ept/docs/document_index.md`
- DEV-STORY-006 acceptance scope: all 31 `foundry_sdk.v2.filesystem` operations, common infra, 5 paginated operations, ACL, exit codes, JSON/TOON/auto output, package/install behavior.
- DESIGN-007 context from linked comments: OP_SPECS pattern, nested `client.filesystem.Resource.Role`, async main, `console_main`, deliverables, 22 AC groups.
- Canonical env var reference and metadata allow-list entries for `filesystem`.

### Local implementation-access check
Commands executed outside tracker internals:
```powershell
rg --files src .claude tests | rg "filesystem|foundry_filesystem|foundry-filesystem"
rg -n "filesystem" pyproject.toml README.md .claude src tests
```
Observed result:
- No `foundry-filesystem` skill package, CLI module, launcher, or tests found under `src`, `.claude`, or `tests`.
- Only generic/common filesystem mentions were found (`config_loader.py`, binary/session tests), not DEV-STORY-006 implementation.
- Linked child tickets confirm `DEV-007: Open` and `UNITTEST-007: Open`.

### Decision
Open -> In Progress DoD is not met. Per testcase Open instructions, QA must not create executable test cases against non-existent implementation files. TESTCASE-007 should be blocked until DEV-007 and UNITTEST-007 complete and filesystem implementation is accessible.

### Pending QA design coverage when unblocked
QA test design must cover:
- 31 filesystem operations across `folder`, `project`, `resource`, `resource_role`, and `space`.
- 5 paginated operations: `folder.children`, `project.organizations`, `resource.markings`, `resource_role.list`, `space.list`.
- Parser/dispatch, nested Resource.Role client routing, common infra integration, exit codes `0/2/4/8/9`, ACL precedence, metadata-only allow-list, JSON/TOON/auto output, stderr pagination metadata, timeout/retry behavior, console entry point, package install/editable install, and `.claude/skills/foundry-filesystem/` launcher behavior.
